# 依赖
node_modules/

# 打包输出（electron-builder 默认目录）
dist/
out/

# 本地数据：正常情况下这些文件在 %APPDATA%\Barbara\，不在项目里。
# 这里再挡一层，防止有人把数据文件拷进项目目录后被误提交。
config.json
conversations.json
characters.json

# 编辑器 / 系统临时文件
*.bak
*.log
npm-debug.log*
.DS_Store
Thumbs.db
.vscode/
.idea/
