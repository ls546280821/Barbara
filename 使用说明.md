# Barbara（芭芭拉）—— 自己动手做一个对话 AI

这是一个**能直接跑的桌面对话 AI**：有自己的窗口、能连续聊天、回答像打字一样一个字一个字冒出来、
可以给它设定性格（人设），聊天记录会自动存在你自己电脑上。

界面上的角色是《原神》里蒙德西风教会的祈礼牧师兼偶像歌手 **芭芭拉**（英文名 **Barbara**），
默认人设已经按她的性格写好了（开朗、温柔、元气满满），你也可以在设置里随时换成别的角色。

你不需要看懂全部代码。**照着下面 5 步做就能用起来**。

---

## 第 0 步：先搞清楚它是怎么工作的

```
你的电脑                          模型服务商的服务器
┌──────────────────────┐          ┌──────────────────┐
│  Barbara 窗口（界面） │          │                  │
│        ↓             │          │   DeepSeek /     │
│  主进程 main.js  ────┼── 网络 ──┤   OpenAI / 通义  │
│        ↑             │  API Key │                  │
│  聊天记录存本地      │          └──────────────────┘
└──────────────────────┘
```

- **界面**负责好看和好用；**主进程**负责发请求、存文件。
- **模型不在你电脑上跑**，而是通过 API 调用别人的服务器，所以不需要好显卡。
- 你需要的只有一个东西：**API Key**（相当于一张充值卡，按用量扣费）。

---

## 第 1 步：装 Node.js（约 5 分钟）

1. 打开 <https://nodejs.org>
2. 下载左边那个 **LTS** 版本（`.msi` 文件）
3. 双击安装，**所有选项保持默认，一路「下一步」**
4. 装完后验证：按 `Win + R`，输入 `cmd` 回车，在黑窗口里输入：

   ```
   node -v
   ```

   出现类似 `v20.11.0` 这样的版本号就成功了。如果提示「不是内部或外部命令」，重启一次电脑再试。

---

## 第 2 步：申请 API Key（约 5 分钟）

以 DeepSeek 为例（便宜、中文好、注册简单）：

1. 打开 <https://platform.deepseek.com>
2. 注册 / 登录
3. 左侧找到 **API Keys** → 创建新的 Key
4. **立刻把 `sk-` 开头的那串字符复制到记事本保存** —— 这个窗口关掉后就再也看不到完整 Key 了
5. 在 **充值 / 账户余额** 页面充一点点钱（几块钱就能聊很久）

> 想用 OpenAI：Key 在 <https://platform.openai.com/api-keys> 申请，接口地址填 `https://api.openai.com/v1`。
> 想用通义/智谱/Kimi 等：都在各自的「开放平台」里申请，接口地址看它们的文档，格式类似。

---

## 第 3 步：启动（约 3 分钟，第一次要下载运行环境）

**最简单**：**双击 `Start-Barbara.cmd`**。它内部会绕过脚本执行策略，所以不管系统有没有禁用脚本都能用。

另外一种等价方式：

- **右键 `Start-Barbara.ps1` → 「使用 PowerShell 运行」** —— 中文提示和报错最完整

它们做的事一样：检查 Node.js → 首次自动装依赖 → 启动 Barbara → 出错时把原因留在屏幕上。

> 启动器文件名特意用了英文：`.cmd` 里的中文（尤其中文文件名）在 cmd 下会被按乱码字节误读，
> 导致「文件明明在却报找不到」。中文说明都在 `.ps1` 里，那边没有这个问题。

**全都不行时的手动方式**：在项目文件夹的地址栏输入 `cmd` 回车，然后依次执行：

```
node -v
npm install
npm start
```

> `node -v` 是为了先确认 Node.js 装好了（能显示版本号才继续）。
> `npm install` 只跑一次，以后都只要 `npm start`。

**第一次启动会黑屏等 1～5 分钟**，因为要下载约 100MB 的 Electron 运行环境，这属于正常现象，别急着强退。
那个黑色命令行窗口是 Barbara 的运行日志，**不要关掉它**，关掉它 Barbara 也会一起退出。

> **连 `node -v` 都报错或闪退？** 那就不是项目的问题，而是这台电脑的运行环境有问题。
> 在一个**已经打开的**命令行窗口里执行（不要双击，双击可能一闪就没）：
>
> ```
> cd /d "<项目所在路径>"
> powershell -NoProfile -ExecutionPolicy Bypass -File Diagnose.ps1
> ```
>
> 它会把系统、Node、npm、项目文件、数据目录的情况汇总成 `info-diagnose.txt`，
> 把这个文件发出来即可定位。

---

## 第 4 步：填 API Key，开始聊天

1. 窗口打开后，点**左下角「设置」**
2. 默认已经有一个 **DeepSeek** 服务商，按顺序填：
   - **名称**：随便写，只是给你自己看的
   - **接口地址**：`https://api.deepseek.com`
   - **API Key**：粘贴第 2 步保存的 `sk-...`
   - **可用模型**：一行一个，默认已经填好 `deepseek-chat` 和 `deepseek-reasoner`
3. 点 **「测试当前服务商」**，看到「连接成功」就说明通了
4. 点 **「保存」**，然后回到输入框，输入「你好」按 Enter

**回答开始一个字一个字往外冒，就成功了。**

---

## 角色扮演：导入角色卡，扮演任何人

这是新加的功能。你可以把酒馆（SillyTavern）的角色卡直接导入进来，让 AI 扮演那个角色跟你聊天。

### 导入一张角色卡

1. 点左下角 **「角色库」** —— 会切到**角色列表页**
2. 点右上角 **「导入角色卡」**
3. 选一个 `.png` 或 `.json` 文件 —— 酒馆的 PNG 角色卡可以直接用，头像、设定、开场白、示例对话都会一起读进来
4. 检查一下内容对不对，点 **「保存角色」**

导入的角色会一直保存在本地，下次打开还在。

### 角色卡上的按钮

角色列表页里每张卡都有：

| 按钮 | 作用 |
| --- | --- |
| **聊天** | **新建一个会话**并绑上这个角色，直接开聊。卡里写了开场白的话，会自动作为角色的第一句话放进去 |
| **编辑** | 打开编辑弹窗改设定 |
| **×**（右上角） | **删除这个角色**。平时是透明的，**鼠标移到卡片上才会浮出来**，免得误点 |

> 删除前会弹确认框，并告诉你「用到它的会话会变回通用助手」。
> 弹窗底部的「删除角色」也还在 —— 正在编辑的时候用那个更顺手。

### 让角色开口

点卡片上的 **「聊天」** 是最快的路子 —— 一下就是新会话 + 绑好角色 + 插好开场白。

> **换角色要开新会话。** 一个会话扮演谁，在它建立的时候就定下来了 ——
> 想跟另一个角色聊，回角色库点那张卡的「聊天」。
> 顶部的 `角色名 · 服务商 · 模型` 会一直告诉你现在在跟谁聊。

### 每个会话记自己的角色

不同会话可以绑不同的角色，互不影响。

想开新对话，就点左侧的 **「＋ 新对话」** —— 它会带你去**角色列表页**，点哪张卡上的「聊天」就是用哪个角色开一局。角色在会话建立时定下来，之后不再变化。

### 自己写一个角色

在角色列表页右上角点 **「＋ 新建角色」**，会打开一张**空白的角色编辑器**，然后填这些字段：

| 字段 | 作用 |
| --- | --- |
| **头像** | **点左上角那个方块就能上传**（见下） |
| 角色名 | 角色叫什么，`{{char}}` 会替换成它 |
| 年龄 / 性别 / 种族 | 身份三项。性别是下拉框，年龄和种族随便写（种族默认「人类」）。**游玩世界书时会进状态面板** —— 因为故事里时间会走，过一年年龄该涨一岁 |
| 角色描述 | 这个角色是谁、长什么样、有什么经历 |
| 性格 | 开朗、爱笑、有点冒失…… |
| 场景 | 你们正在哪里、在干什么 |
| 开场白 | 第一句话，绑定角色时会自动插入 |
| 示例对话 | **最有用的一个**，写几组对话能显著提升语气准确度 |
| 标签 | **分类用**，逗号分隔，比如 `原神, 治愈, 日常`。填了会显示在角色卡上（前 3 个），方便一眼看出这张卡属于什么类型 |
| 属性（状态面板） | 见下面「状态面板」那一章 —— 声明这个角色要维护哪些数值 |

> **新建的角色要点「保存角色」之后才会真的出现。**
> 在那之前它只是编辑器里的一张**草稿** —— 角色列表里看不到它，也不会写进
> `characters.json`；标题旁边和底部也会一直提醒你「还没保存」。
> 这时如果关掉弹窗（点 ✕、按 Esc、点弹窗外），会先问你一句要不要放弃：
> 确认放弃就等于没建过，什么都不会留下。
>
> 已经在角色库里的角色不受影响 —— 点卡片上的「编辑」改完保存，还是原来的那张卡。

**上传头像**：在角色编辑区左上角，**直接点那个方框**就会弹出文件选择框
（鼠标移上去会显示「换头像」）。支持 PNG / JPG / WebP / GIF / BMP。
传完之后右边的「清除头像」按钮就会亮起，想撤掉点它。

> 头像会被**自动压成 256×256 的方图**再保存 —— 原图几 MB 的话会让角色库文件
> 涨到几十 MB，压过之后通常只有几十 KB，而且不管传什么比例的图都不会变形
> （居中裁成正方形）。
>
> 改完头像记得点右下角**「保存角色」**，否则不会生效。

「高级选项」里还有三个字段，一般不用管：

- **角色专属系统提示词**：填了会取代「设置」里的全局人设
- **对话后指令**：放在整段对话最后，用来强调格式或语气要求
- **作者备注**：只给你自己看，不会发给模型

### 示例对话怎么写

这是提升扮演质量最有效的一招。格式是这样，每行以 `{{user}}:` 或 `{{char}}:` 开头：

```
{{user}}: 你好呀
{{char}}: 你好～今天天气真好呢，要不要一起去散步？
{{user}}: 你不是要唱歌吗
{{char}}: 啊，对哦！（拍拍脑袋）差点忘了，等下要去教堂排练，你要来看吗？
<START>
{{user}}: 你最喜欢什么颜色
{{char}}: 嗯……蓝色吧！就跟蒙德的天空一样。
```

`<START>` 用来分隔不同的对话组，可以写好几组。程序会把它们当成「已经发生过的示例对话」一起发给模型，模型照着这个语气说话就会准很多。

### 宏：`{{char}}` 和 `{{user}}`

角色卡里到处是这两个占位符：

- `{{char}}` → 替换成角色名
- `{{user}}` → 替换成「你」——名字在**设置 → 人设 → 你的名字**里改

在角色描述、性格、场景、开场白、示例对话里都可以用。

### 上下文轮数

设置 → 人设 → **「带入轮数」**，默认 20 轮（一问一答算一轮）。

角色扮演建议**调大一点**，比如 40～60，不然 AI 会忘掉前面的剧情。代价是每轮都要把更多历史发给模型，**更费 token**。

### 两个必须知道的行为

- **绑定了角色的会话不再使用全局人设**。这是故意的：否则你扮演雷电将军，系统提示词却在说「你是芭芭拉」，模型会精神分裂。想让角色用通用人设，就在角色卡里留空「角色专属系统提示词」。
- **删掉角色后**，用到它的会话会自动变回通用助手，不会报错。

---

## 世界书：一整个世界

角色卡是「AI 扮演**一个人**」，世界书是「AI 扮演**整个世界**」。

一本世界书里装三样东西：

- **设定条目** —— 一堆带关键词的设定。只有关键词出现在最近的对话里，那条才会被塞给模型，没提到的完全不占 token。所以你可以往里塞几万字，平时几乎不花钱。
- **角色副本** —— 这个世界里的 NPC。它们是独立个体，改它们不影响角色库里的原角色。**它们的名字和设定会一直告诉 GM**，GM 才知道这个世界有谁、该扮演谁。
- **开场白** —— 进这个世界时自动作为第一条消息。留空的话，进去时让 AI 按设定现生成一段开局场景。

### 玩一个世界：四步

1. 左下角点 **「世界书」**
2. 在列表里找到想玩的那本，点卡片上的 **「游玩」**
3. **创建你自己的角色** —— 给这个世界里的自己起个名字，再写段设定（你是谁、长什么样、什么来头、会什么）。名字预填设置里的「你的名字」，改一下就行
4. 点 **「开始游玩」**

> **想直接用角色库里的人物当自己？** 第 3 步最上面有个下拉框，
> 选一张角色卡就行 —— 名字和设定会**自动填到下面**（填完还能改，改了以你改的为准），
> 这张卡上的**「属性」和「年龄 / 性别 / 种族」也会一起带进状态面板**。
> 选「（自己写一个）」就还是手填，什么都没变。

进去之后会自动帮你做好这些事：

- 新建一个会话，并把这个世界的设定装上
- 打开 **GM 模式**（AI 负责描写环境、扮演所有 NPC，你当主角）
- 把你的角色写进提示词，让 GM 知道主角是谁；`{{user}}` 也替换成你的角色名
- **把这本书里的 NPC 列给 GM**（见下面「往世界里放 NPC」），它才知道这个世界有谁
- **不再使用「设置 → 人设」里的全局人设** —— 世界模型里 AI 是这个世界，不该顶着某一个人的性格说话
- **开场**：书里写了「开场白」就直接用它；没写就让 AI 按这个世界的设定现生成一段开局场景，大概几秒。生成失败也不影响，直接开聊就行

会话顶部会显示 `· 世界：书名 · GM 模式`，一眼知道自己在哪个世界；每条回复的说话人也会显示成**世界名**，你自己的消息则显示成你创建的角色名。

### 写设定条目

在**世界书列表页点卡片上的「编辑」**进编辑器，然后：

1. 点 **「＋ 条目」**
2. 填三样东西：
   - **条目名称**：只给你自己看，比如「小镇广场」
   - **关键词**：逗号分隔，比如 `广场, 喷泉, 告示牌`
   - **内容**：命中时要告诉 AI 的设定，写成一个能独立看懂的段落
3. 点 **「保存条目」**

> 关键词命中的规则：**中文按子串匹配**（写「广场」，「小镇广场上」也能命中）；
> **英文默认也是子串匹配**，勾「全词匹配」才按词边界。也可以写 `/正则/`。

### 往世界里放 NPC

编辑器里有一块 **「本书角色」**：

- **＋ 从角色库加入** —— 把角色库里已经做好的角色**复制**一份进来，之后两边各改各的
- **＋ 新建** —— 打开角色编辑器现写一个。和角色库一样，点「保存角色」之后才会加进这本书（没保存就关掉等于没建过）

这些副本就是**这个世界里的人**。它们的名字、描述和性格会**每轮都告诉 GM**，所以 GM 知道有哪些 NPC、各自什么脾气，能直接让他们登场和你对话 —— **不用你先提到名字**。

> 正因为是每轮都注入，别放太多人、描述也别写太长（有总长上限，超了会被截断）。

### 开场白

同一块面板下面有 **「开场白」** 一栏：写了就会在你进这个世界时，原封不动作为第一条消息出现（支持 `{{user}}`，会替换成你的角色名）。

留空的话，进去时让 AI 按设定现生成一段开局。**建议还是自己写** —— 你能控制故事从哪儿开始，而且开场白里提到的地名、人名会自动触发对应的设定条目，等于一进门就把世界点亮了。

### 怎么确认设定真的生效了

编辑器里有 **「预览命中」** 按钮。它拿当前会话最近的聊天记录，跑一遍**和真实请求完全相同**的匹配逻辑，告诉你这本书这一轮会注入哪几条。

**这是排查「为什么没生效」最快的办法**，比反复发消息试要省得多。没命中的常见原因：关键词写错了，或者关键词压根没出现在最近 6 条消息里。想让某条设定永远在场，就勾上 **「常驻条目」**。

### 换个主角重玩

「你的角色」是**存在会话里**的。想用另一个身份重进同一个世界，回世界书列表页再点一次「游玩」就行 —— 会开一个新会话，两个存档互不影响。

---

## 视角设置：让 AI 跳出单角色

聊天窗口右上角的 **「视角」** 按钮。设置**跟着会话走**，随时可改，改完从下一条回复生效。

### GM 模式（主持人视角）

**关掉时**，AI 只会扮演单个角色，而且被一条硬性规则限制住：*「始终以第一人称……不要跳出角色」*。这就是为什么普通模式下 AI 不会描写环境、不会演别的 NPC。

**打开后**，这条规则会换成「主持规则」：AI 扮演**整个世界和所有 NPC**，用第三人称描写环境，把你当作故事主角。

- 玩**大世界模型**：打开
- 玩**单角色角色扮演**：关掉（默认）

### 叙述模式

| 模式 | 效果 |
| --- | --- |
| **标准** | 只写对话和动作（默认） |
| **内心描写** | 正文 + 角色的真实心理 |
| **上帝视角** | 正文 + 心理 + 旁白，三个层次 |

选好之后，格式要求会**自动加进提示词**，你不用手写。模式是**按会话记的**，新开的会话默认回到「标准」。

### 心理描写长什么样

AI 写出的 `【心理】` 和 `【旁白】` 会被**渲染成独立样式块**，和正文一眼分开：

- **【心理】** —— 蓝色左边框 + 淡蓝底 + 斜体灰字
- **【旁白】** —— 紫色虚线框 + 淡紫底 + 斜体灰字

标记本身不显示出来（有样式就不需要文字占位）。

> 这是**软件渲染**做的，跟提示词无关。你手动在角色卡的「对话后指令」里写
> `心理活动用「【心理】」开头单独成段` 也能触发同样的样式。

### 重点标出来（不然长段落看得累）

回复里可以有两种重点样式，软件会自动渲染：

| 写法 | 效果 | 用在哪 |
| --- | --- | --- |
| `**文字**` | **加粗** | 关键信息：数值、名字、时间地点、重要决定 |
| `==文字==` | **加粗 + 变蓝 + 一点底色** | 真正不能错过的：危险、转折、关键线索、承诺 |

标记符号本身不会显示出来。

**AI 会自动用**：扮演规则和主持规则里都写了「关键信息用 `**`、最关键的用 `==`，
一篇里标一两处就够」，所以不用你每次交代。它标得不合心意时，也可以在角色卡的
「对话后指令」里自己补一句（比如 `重要的话用 ==高亮== 标出来`）。

> **你自己写的内容也认这套标记** —— 世界书的「开场白」、角色卡的「开场白」
> 都会原样渲染。想在一大段开场里点出重点，直接写 `==天黑了，别出门。==` 就行。

### 要是还嫌不够清楚：调对话窗口

右上角 **「外观」** 里三样东西，改完**立即生效**，不用点保存：

| 设置 | 说明 |
| --- | --- |
| **正文字号** | 12–22px 的滑块。标题、表格、心理/旁白会跟着一起缩放，不是只有正文变大 |
| **加粗字的颜色** | 觉得光加粗还不够跳，就给它上个色。左边是**色盘**，右边可以直接填 `#e06c75`（不带 `#` 也认） |
| **对话窗口背景图** | 铺在消息区当壁纸。图片会先压到最长边 1920 再存 |

关于这三样：

- **`==高亮==` 不受加粗颜色影响** —— 它有自己的一套（主题色 + 底色）。两者可以配着用：
  比如加粗设成暖色，高亮还是蓝色，一眼就能分出两档。
- **背景图下面还是实心气泡**，所以文字照样清楚，不用担心挑了一张花的图就看不清字。
- 颜色留空（或点「默认」）就跟随主题色，深浅色模式各用各的。
- 这些**只影响显示**，一个字都不会发给 AI。

---

## 状态面板：让数值不再漂

世界模型常带一个状态栏：

```
【金币】：100
【时间】：早上
【地点气候】：小镇，天气晴朗
```

**问题**：模型是靠「抄自己上一轮」来记住这些数值的。而 Barbara 只把最近 N 轮发给模型（设置里的「带入轮数」）—— **超出去的轮次模型就看不见了，于是开始编数值**。

**解决**：Barbara 会把状态栏**解析出来单独保存**，每轮由程序权威注入。这样不管聊多少轮，数值都不会漂。

### 怎么用

不需要配置。**AI 回复里出现「【字段】：值」格式的行，就会自动被识别**，然后：

- 消息区上方出现 **「状态」** 面板，列出所有字段
- **值可以直接点进去改**，改完立刻生效
- 悬停某一行可以删掉字段
- 面板右上角 **「重置」** 清空，下一条带状态栏的回复会重新建立

### 默认收起，随时展开

面板**默认是收起的**（不挡着看故事），只留一条细条：

```
› 当前状态  8/8 项已填                    [重置]
```

- **点它一下就展开**（左箭头 + 「当前状态」那一整条都能点，不用瞄准小按钮）
- 再点一下就收起。箭头方向就是状态：**︿ 展开着 / › 收起了**
- 收起的是**这一条**，不会整个消失 —— 所以随时都有地方点开，不会「收起来就找不回来」
- 切换会话时**回到默认收起**，同一个会话里你手动展开的状态会保留

### 更好的用法：在角色卡上先声明属性

上面那种「等 AI 自己冒出来」有个毛病 —— **开局它未必想得到该维护哪些字段**。
所以可以在**角色编辑器**里先把属性声明好：

1. 打开角色编辑弹窗，找到 **「属性（状态面板）」** 一栏
2. 手写一个属性名，或者点下面的**快捷候选词**（金币、生命、时间……）加进来
3. 给每个属性填一个**初始值**（可以留空）
4. 点「保存角色」

**一次加很多条？用「粘贴文本」。** 点属性区右边的**「粘贴文本」**，把一段文本贴进去，
点「解析并加入」就批量生成好了。一行一项，认这几种写法：

```
金币：9900
【上衣】：衬衫
年龄 16
- 下装：裙子          ← 列表符号会自动去掉
```

- **同名会覆盖值**，不会加出第二条；认不出来的行自动跳过
- 结果会用提示条告诉你：`已解析：新增 5 项，更新 1 项，跳过 2 行`
- 状态栏的**保留字**（心理、旁白、扮演规则……）不会被收进来

之后**绑上这个角色**（或者进入放了这个角色的世界书）时，这些属性会自动种进状态面板，
并且**第一轮就告诉 AI 要维护哪些字段** —— 不用再等它碰巧输出。

> **初始值 vs 当前值**：角色卡上存的是**初始值**，进游戏之后在面板里改的是**那一局的当前值**。
> 两者分开，所以改角色卡不会影响正在进行的游戏。
>
> **快捷候选词在哪改**：设置 → **状态属性** → 常用属性（逗号或换行分隔）。
> 它只是省打字的清单，不限制你手写别的属性名。

### 身份四项（姓名 / 年龄 / 性别 / 种族）

卡上的**年龄 / 性别 / 种族**不是摆设 —— 它们会以两种方式告诉 AI：

**① 角色信息里**（单角色对话也生效）：
```
【昔涟的基本信息】
年龄 16，性别 女，种族 人类
```
这是最可靠的一条：不管面板在不在，AI 一开始就知道。**不填的话它会自己编**
（实测：卡上写 16 岁，回复里却成了 21 岁）。

**② 状态面板里**，绑上角色（或进世界）时自动种进去，因为世界里时间会走、
剧情会推 —— 过一年年龄该涨一岁，被人改了名字也得跟着改：

- **单角色对话**：种的是**被绑那张卡**的身份（姓名 = 角色名）
- **游玩世界书**：种的是**你自己**的身份（姓名 = 你在弹窗里填的名字，其余来自选的卡）
- 没选角色卡就只有姓名 —— 手填的玩家没有年龄/性别/种族
- **剧情改了面板里的「姓名」，消息上的名字标签和 `{{user}}` 会跟着变**

**NPC 的身份**进的是「这个世界的人」名单（`艾莉（16岁·女·精灵）：……`），
不占状态面板 —— 一局有好几个 NPC，都塞进去会出现「五个【姓名】」互相覆盖。

### 设计上的几个细节

- **状态栏仍然显示在消息里**（方便你回看当时的数值），但**发给模型时会剥掉** —— 避免和历史里那份重复、冲突
- **面板块紧贴对话历史之后**注入，位置权重很高
- 一行里出现**长句子**不会被当成字段，所以正文提到「【某某】」不会污染面板
- 面板字段**会自动累积** —— AI 后面新加的字段（比如「新的线索」）会被自动收进来
- 属性名不能用状态栏的**保留字**（心理、旁白、扮演规则……），那些是提示词自己的段落标记

---

### 排查：为什么没生效

点弹窗里的 **「预览命中」**。它会拿**当前会话的真实对话**扫一遍，告诉你这一轮会注入哪些条目、一共多少条。

如果显示「一条都没命中」，说明关键词没写对 —— 想想你会不会真的在对话里打出这个词。

### 常驻条目

不想靠关键词触发、希望每轮都注入的设定（比如「这个世界有魔法和蒸汽机」），勾上 **「常驻条目」**。注意常驻条目每轮都占 token。

### 递归扫描（让设定自己串起来）

**问题**：一本书越写越厚之后，你得**主动提到**某个词，那条设定才会被带进来。你写了「十二泰坦」的详细设定，但正文里只说了「翁法罗斯」—— 那 AI 就完全不知道十二泰坦是什么。

**解决**：勾上条目里的 **「递归」**。这条命中之后，**它的正文也会被拿去再匹配一遍**，于是正文里提到的词能触发别的条目，一路连锁：

```
你说「翁法罗斯是个什么样的地方？」
  → 命中【世界总览】
    → 它的正文里有「十二泰坦」        ← 递归再扫一遍
      → 命中【十二泰坦】，正文里有「火种」
        → 再命中【火种】……
```

不用你手动一个个提，一轮就把整条知识链铺开了。

**两道闸，防止烧 token**：

| 闸 | 在哪 | 说明 |
| --- | --- | --- |
| 哪些条目往下带 | 条目里的「递归」勾 | **默认不勾**。只给「总览 / 索引 / 目录」这类条目开 |
| 最多连锁几层 | 设置 → 行为 → 「世界书递归深度」 | 默认 3，`0` = 完全关掉递归 |

> 递归会**明显增加 token**（每多一层就多塞几条设定），所以别给细节条目也开。
> 想看效果：世界书编辑器里的 **「预览命中」** 会告诉你这次带出了几条、其中几条是递归来的。

### 从角色卡导入的世界书

酒馆导出角色卡时，会把角色绑定的世界书**一起打包进 PNG**。导入这种角色卡时，应用会把内嵌的世界书自动解析出来，存成一本**独立的世界书**。

> 注意：**它不会自动生效**。导入后它只是一本躺在列表里的书 —— 想玩它就回世界书列表页点那张卡的「游玩」。
>
> 之所以不自动绑：世界书是独立实体，一本书可以服务很多个会话；自动绑反而会在你没注意的时候往对话里塞设定。

> 顺便说：内嵌世界书这块，**早期版本是直接丢掉的**。如果你有旧卡感觉「角色失忆」，重新导入一次就能把书找回来。

---

## 分支与存档点：走另一条剧情线

跑到一个岔路口，想看看另一种选择会怎样 —— 但直接把对话往回改，之前那条线就没了。

这里给了**两个**手段，区别在**代价**：

| | 在哪 | 做什么 | 代价 |
| --- | --- | --- | --- |
| **分支** | 鼠标悬停在任意一条消息上 → 「分支」 | **另开一个会话**，把这条及之前的原样复制过去 | **什么都不丢**。原来那条线一个字节都没动，侧栏里两条并排，随时切回来对比 |
| **存档点** | 「记忆」弹窗 → 「存一个档」 | 在当前会话里存一份快照 | 读档会把对话**还原成存档时的样子**，存档之后聊的会丢 |

**什么时候用哪个**：

- 「我想试试另一条路，但**不想失去现在这条**」→ **分支**
- 「这一段我想反复试几种写法」→ **存档点**（存一次，读档可以反复来）

**分支会带走什么**：绑的角色、世界书、你的玩家角色、状态面板、视角设置，还有前 N 条消息。
**不带走摘要** —— 摘要压缩的是「最早那批消息」，而新会话里这批消息是原样留着的，搬过去等于同一段内容被记两遍，所以新线从零开始攒记忆。

> 存档点最多留 12 个（每个都是整段对话的副本，攒多了会把存档文件撑大）。

---

## 记忆：聊多久都不会忘了开局

**问题**：Barbara 只把最近 N 轮发给模型（设置里的「带入轮数」）。聊到第 50 轮，第 3 轮发生的事模型**完全看不见了** —— 于是角色开始忘事、前后矛盾。调大轮数就烧 token，调小就忘得快，这是个死结。

**解决**：把较早的对话**按段压缩成摘要**。摘要一直留在上下文里，原文丢掉。

效果是：**几十轮前的剧情还在，但 token 占用小得多。**

### 怎么用

**不用配置，自动的。** 未压缩的对话攒到 24 条（约 12 轮）时，Barbara 会在回复完成后**后台自动压一段** —— 不打断你聊天。

顶部 **「记忆」** 按钮上的数字是已压好的段数，点进去可以：

| 操作 | 说明 |
| --- | --- |
| **编辑** | 直接改摘要内容。AI 写漏了关键信息可以自己补 |
| **重新生成** | 拿这段对应的原文重压一次 |
| **删除** | 删掉这段，对应原文重新进入上下文 |
| **立即压缩** | 不想等阈值，手动压一段 |
| **清空全部摘要** | 全部还原成原文（token 占用会明显上升） |

### 几个设计细节

- **原对话一个字都没删**。摘要只是「代替原文发给模型」，消息记录里你随时能回看
- **上下文是连续的**：摘要覆盖了前 30 条，原文就从第 31 条开始发 —— 不会出现「摘要到 30、原文从 70 起」这种中间断层
- **摘要写得简洁**：提示词明确要求「只记事实、不要文学化」，因为摘要是常驻的，写成抒情散文既费 token 又容易让模型把摘要当剧情续写
- **失败会退让**：连续失败 3 次就暂停自动摘要，不会每轮都白烧一次请求

> 为什么这件事必须由程序做：让模型「自己记住」是不可靠的 ——
> 它每次只能看到发给它的那部分上下文，看不到的东西就是真的不知道。

---

## 多模型：同时配置好几个 AI，随时切换

这是这个版本的重点功能。你可以**同时配好 DeepSeek、OpenAI、通义千问、智谱、Kimi 等等**，
聊天时在右上角的下拉框里一键切换，用哪个模型对比哪个模型的回答。

### 添加一个服务商

1. 设置 → 「模型服务」→ 点 **「＋ 添加服务商」**
2. 从弹出的预设里点一个（DeepSeek / OpenAI / 通义千问 / 智谱 GLM / Kimi / 自定义）
   —— 接口地址和常用模型会自动填好
3. 填上这个服务商的 **API Key**（每家都要自己的 Key）
4. 点 **「保存」**

### 切换模型

**聊天窗口右上角有个下拉框**，里面按服务商分组列出了所有模型：

```
┌─────────────────────────┐
│ DeepSeek                │   ← 一组一个服务商
│   deepseek-chat         │
│   deepseek-reasoner     │
│ OpenAI                  │
│   gpt-4o-mini           │
│   gpt-4o                │
└─────────────────────────┘
```

选一个就立刻生效，不用进设置。切换后：

- **当前会话**改用新模型，之后的回答都由它来答
- 这个选择同时会成为**新会话的默认模型**
- 每条 AI 回答的左上角会标出**是哪个模型答的**，方便对比

### 每个会话记住自己的模型

不同会话可以用不同的模型：在 A 会话选 `gpt-4o`，切到 B 会话选 `deepseek-chat`，
两边互不影响。标题下面那一行会显示当前会话正在用哪个服务商的哪个模型。

### 拉取模型列表

不想手动敲模型名？点 **「拉取可用模型」**，程序会问服务商要一份完整列表并自动填进去
（最多填 120 个，多余的可以自己删）。有些服务商不提供这个接口，那就手写模型名，一行一个。

---

## 已经做好的功能

| 功能 | 在哪 |
| --- | --- |
| **角色扮演 / 角色卡** | 左下角「角色库」是**角色列表页**；卡片上「聊天」直接开新会话 |
| **导入酒馆 PNG 角色卡** | 角色列表页 →「导入角色卡」 |
| **每个会话独立绑角色** | 自动，标题下方显示当前角色 |
| **世界书（Lorebook）** | 左下角「世界书」是**世界列表页**；关键词命中才注入，四两拨千斤 |
| **玩一个世界** | 世界书列表页 → 点卡片上的「游玩」→ 创建你自己的角色 |
| **内嵌世界书自动提取** | 导入带 character_book 的角色卡时自动解析并绑定 |
| **世界书命中预览** | 世界书弹窗 → 「预览命中」，用真实对话扫一遍 |
| **状态面板** | 自动识别回复里的「【字段】：值」，每轮权威注入，数值不漂 |
| **面板手动改值** | 面板里直接点输入框改，立刻生效 |
| **角色属性（属性模板）** | 角色编辑器里先声明字段 + 初始值，绑定时种进状态面板，开局 AI 就知道要维护什么 |
| **导出** | 角色卡存成 PNG 卡（酒馆能直接导入）或 JSON；世界书存成 lorebook JSON；对话存成 Markdown |
| **递归扫描** | 条目里勾「递归」→ 它的正文还能再触发别的条目，一路连锁（深度在设置里调） |
| **候选（swipe）** | 「重新生成」不会扔掉上一条 —— 老的留着，角色名旁边出现 `‹ 2/3 ›` 左右翻着挑 |
| **分支 / 存档点** | 消息上「分支」= 另开一个会话把前半段照搬（原会话不动）；记忆里的「存档点」= 当前会话的快照，可读档退回 |
| **分段记忆摘要** | 自动；较早的对话压成摘要常驻，聊多久都不忘开局 |
| **记忆管理** | 右上角「记忆」；可编辑 / 重新生成 / 删除 / 清空摘要 |
| **叙述模式** | 右上角「视角」→ 标准 / 内心描写 / 上帝视角（按会话记） |
| **GM 模式（主持人视角）** | 右上角「视角」→ 勾选；让 AI 扮演整个世界和所有 NPC |
| **心理 / 旁白样式** | 自动渲染成独立样式块，与正文区分 |
| **重点样式** | `**加粗**` / `==高亮==`（加粗+变色），AI 会自动用，你自己写的内容也认 |
| **对话窗口外观** | 右上角「外观」：正文字号、加粗字颜色、消息区背景图，改完立即生效 |
| **{{char}} / {{user}} 宏** | 角色卡里直接写，发送时自动替换 |
| 多模型 / 多服务商 | 设置 → 「模型服务」添加；窗口顶部的下拉框切换 |
| 每个会话独立记模型 | 自动，标题下方显示当前用的模型 |
| 回答上标注模型名 | 每条 AI 回答左上角的小标签 |
| 多轮对话（记得上下文） | 默认 20 轮，可在设置里调 |
| 流式输出（打字机效果） | 默认就有 |
| 自定义人设 | 设置 → 「人设 / 系统提示词」（未绑角色时生效） |
| 历史会话保存与切换 | 左侧列表，点标题切换 |
| 新建 / 删除会话 | 「＋ 新对话」/ 悬停会话后的 `×` |
| 复制单条 / 整段对话 | 鼠标悬停在消息上，或右上角「复制全文」 |
| **删除单条消息** | 鼠标悬停在某条消息上 → 「删除」（会先确认） |
| **改某条消息** | 鼠标悬停在消息上 → 「编辑」→ 直接改原文 → 保存（**Ctrl + Enter** 保存 / **Esc** 取消） |
| **接着往下写** | 回复被截断时，鼠标悬停在**最后一条**回复上 → 「继续」 |
| 重新生成回答 | 悬停在 AI 消息上 |
| 停止生成 | 生成过程中右上角会出现红色按钮 |
| 思考过程折叠展示 | 用 `deepseek-reasoner` 这类模型时自动出现 |
| 用量统计（可关闭） | 输入框右下角；设置 →「行为」→「显示 token 使用量」 |
| **白天 / 夜间模式** | 左上角「Barbara」右边的小图标，点一下切换 |
| API Key 加密存储 | 自动，保存在本机且加密 |

---

## 常用操作

- **发送**：Enter；**换行**：Shift + Enter
- **切换白天/夜间**：左上角品牌名右边的小图标（月亮/太阳），选择会记住，下次启动还是这个模式
- **想让它换行发送**：设置里取消勾选「Enter 直接发送」，之后用 Ctrl + Enter 发送
- **换模型**：聊天窗口右上角的下拉框
- **换角色**：回左下角「角色库」，点另一张卡上的「聊天」（会开新会话）
- **导入/编辑角色**：左下角「角色库」→ 卡片上「编辑」
- **删除角色**：左下角「角色库」→ 鼠标移到卡片上 → 点右上角的 `×`
- **加世界设定**：左下角「世界书」→ 卡片上「编辑」→ 加条目（想玩就点「游玩」）
- **删除世界书**：左下角「世界书」→ 卡片上「编辑」→ 编辑器里的「删除本书」
  （世界书里可能攒了很多条目和 NPC，删掉找不回来，所以**删除入口刻意留在编辑器里，不在列表页**）
- **让 AI 写心理 / 旁白**：右上角「视角」→ 叙述模式
- **字太小 / 加粗不够显眼 / 想要壁纸**：右上角「外观」→ 字号、加粗字颜色、背景图
- **玩大世界**：右上角「视角」→ 勾 GM 模式；会话不绑角色
- **用现成的角色卡当自己**：世界书列表页 →「游玩」→ 最上面的下拉框选一张卡（名字、设定、属性自动带上）
- **看 / 改状态数值**：消息区上方「状态」面板
- **给角色定属性**（金币、好感度、装备……）：角色编辑器 →「属性（状态面板）」→ 点快捷候选词或手写名字 → 填初始值
- **改属性快捷候选词**：设置 →「状态属性」→ 常用属性
- **角色开始忘事**：右上角「记忆」看看摘要压了没；也可以直接编辑摘要补充关键信息
- **改「你」的名字**：设置 → 人设 → 你的名字（影响 `{{user}}`）
- **AI 太容易忘事**：设置 → 人设 → 把「带入轮数」调大
- **删掉某一条消息**：鼠标移到那条消息上 → 点「删除」
- **改 AI 写错的地方**：鼠标移到那条消息上 → 点「编辑」→ 改完 Ctrl + Enter 保存
- **AI 的话没写完**：鼠标移到**最后一条**回复上 → 点「继续」，它接着往下写（不会重开头）
- **对这条回复不太满意**：鼠标移到那条上 → 点「重新生成」。**老的那条不会丢** —— 角色名旁边会出现 `‹ 2/3 ›`，左右翻着挑，挑到满意为止
- **想走另一条剧情线**：鼠标移到分岔点那条消息上 → 点「分支」。会**另开一个会话**把前半段照搬过去，**原来那条一个字都不动**，侧栏里两条并排，随时切回来对比
- **想先存一下再往前写**：记忆弹窗 → 「存一个档」；写崩了点「读档」退回来（**读档会丢掉存档之后聊的**）
- **把角色卡发/备份给别人**：角色编辑器右下角「导出」→ 存 `.png` 就是一张完整的酒馆卡（带头像和数据），存 `.json` 就是纯数据
- **导出世界书 / 对话**：世界书编辑器里的「导出」、聊天右上角的「导出」（对话存成 Markdown）
- **不想看 token 数**：设置 →「行为」→ 取消勾选「显示 token 使用量」
- **改性格**：设置 → 「人设 / 系统提示词」，比如
  `你是一位耐心的初中数学老师，用生活里的例子解释概念，每次回答不超过 200 字。`
- **改错人了想重来**：悬停在 AI 的回答上 → 「重新生成」

> 多模型的一个实用玩法：同一个问题，先在 A 模型问一遍，再切到 B 模型重新问一遍，
> 对比两边的回答。切换只影响之后的对话，已经生成的回答不会被改动。

---

## 想改成自己的样子

| 想改什么 | 改哪个文件 | 怎么改 |
| --- | --- | --- |
| 配色 | `renderer\style.css` 最上面 `:root` | 把 `--accent` 换成你喜欢的颜色代码；夜间模式改下面 `[data-theme='dark']` 那一块 |
| 窗口标题 | `renderer\index.html` 的 `<title>` 和 `<h1>` | 直接改文字 |
| 默认人设 | `main.js` 里的 `systemPrompt` 一行 | 改引号里的中文（绑定了角色卡的会话不走这里） |
| 默认接口地址 / 模型 | `main.js` 里的 `DEFAULT_BASE_URL`、`DEFAULT_MODEL` | 改成你常用的服务商 |
| 默认记住多少轮 | `main.js` 的 `DEFAULT_SETTINGS.maxTurns` | 用户也可以在设置里自己改 |

### 开发的时候怎么改（保存即刷新）

**这个项目没有构建步骤**——没有 webpack / vite，`renderer/` 里的文件是浏览器直接从磁盘加载的。
所以开发体验跟写静态页面几乎一样，不需要「编译」这一步。

```bash
npm run dev        # 等价于 electron . --dev
```

这条命令会自动做两件事：

1. **打开 DevTools**（独立窗口，跟浏览器 F12 那个面板完全一样）
2. **监听 `renderer/` 目录**，文件一保存就自动刷新窗口

于是改界面的流程就是：

```
npm run dev  →  改 style.css / renderer/js/ 里的文件 / index.html  →  Ctrl+S  →  自动刷新看到效果
```

**跟浏览器开发的区别：**

| 场景 | 怎么做 |
| --- | --- |
| 改 CSS | 保存即刷新。也可以在 DevTools 的 Elements 面板里实时试，满意了再抄回 `style.css` |
| 改 `renderer/js/` 里的文件 / `index.html` | 保存即刷新。**但页面状态会重置**（会话重新加载，输入框里没发出去的内容会丢） |
| 改 `main.js` / `preload.js` | **必须重启**：Ctrl+C 停掉，再 `npm run dev`。这两个文件只在启动时读一次 |
| 看日志 | 主进程的 `console.log` 打在**终端**；页面的打在 **DevTools → Console** |
| 断点 | 页面代码在 DevTools 里打断点；主进程代码需要 `--inspect` 参数配合 |

**打开 DevTools 的三种方式：**

| 方式 | 说明 |
| --- | --- |
| `npm run dev` | 启动时自动打开（最省事） |
| **`F12`** | 在窗口里按，开 / 关切换 |
| `Ctrl + Shift + I` | Electron 默认菜单的快捷键，等价于上面的 F12 |
| `Alt` → 「视图」→「切换开发者工具」 | 菜单栏被隐藏了，按一下 `Alt` 会显示出来 |

刷新窗口是 `Ctrl + R`。这些都不依赖 `npm run dev`，普通 `npm start` 下也能用。

> ⚠️ **注意**：右键菜单里**没有**「检查元素」。Electron 默认不提供右键菜单，
> 这点跟浏览器不一样，别在窗口里右键找。用上面的快捷键。

> **为什么不做热更新（HMR）？** 因为那样就得引入打包器，把「零构建」这个最大的便利丢掉。
> 现在改 CSS 是重新读文件，速度上已经很接近即时了。真要上 HMR，用 Vite + Electron 的模板重搭更合适。

### 改完怎么确认没弄坏（冒烟测试）

```bash
npm run smoke
```

它会开一个**不显示**的窗口，加载**真实的**界面，但把所有读写都换成内存里的假数据 ——
所以它**不会碰你的角色和聊天记录**，随便跑。

测试用「点真实按钮 + 读真实 DOM」的方式过一遍主要流程：启动、主题切换、设置弹窗、
角色库（新建 / 放弃新建 / 编辑已有）、聊天发送与流式回复、世界书条目、本书角色、
玩家角色弹窗，一共 **10 个场景 / 37 条断言**。

- 全过 → 打印 `✅ 冒烟测试通过`，退出码 0
- 有失败 → 打印 `✗ 哪一条  ← 实际值`，退出码 1

> 加新功能时，顺手在 `tools/smoke-renderer.js` 里补一条场景，比每次手点一遍可靠。
> 具体断言清单见 `重构方案.md` 第 0 步。

---

## 你的数据存在哪

聊天记录和 API Key 都**只存在你自己的电脑上**，不会上传到别的地方。位置：

```
C:\Users\你的用户名\AppData\Roaming\Barbara\
├── config.json          设置（含 API Key，已经加密，但也别把这个文件发给别人）
├── conversations.json   所有聊天记录
└── characters.json      角色库（导入的角色卡都在这）
```

> **从旧版 Cyrene 升级过来的用户**：程序第一次启动时会自动把
> `AppData\Roaming\Cyrene\` 里的 `config.json` 和 `conversations.json`
> 复制到新的 `Barbara\` 目录，设置和聊天记录都不会丢。
> 确认没问题后，旧的 `Cyrene` 文件夹可以自己删掉。

窗口里点**左下角「打开数据文件夹」**可以直接跳过去。
想手动备份聊天记录，复制 `conversations.json` 就行。
每次保存前还会自动生成一份 `.backup` 备份，主文件万一损坏会自动从备份恢复。

---

## 遇到问题怎么办

| 现象 | 原因和处理 |
| --- | --- |
| `node -v` 没反应 | Node.js 没装好，重装一次，注意勾选「Add to PATH」，装完重启电脑再试 |
| `npm install` 卡住不动 | 网络问题。等 5 分钟，或换个网络，或先设国内镜像（见下方三条命令） |
| 装依赖时报 Electron 下载失败 | 只设 npm 镜像不够，Electron 本体要单独设镜像（见下方） |
| 提示 `401 未授权` | API Key 填错、多了空格，或者已经被删除。重新复制粘贴一次 |
| 提示 `402 余额不足` | 去服务商后台充值 |
| 提示 `404 找不到接口` | 「接口地址」写错了。DeepSeek 是 `https://api.deepseek.com`，注意不要自己加 `/v1` |
| 提示 `连接超时 / 域名解析失败` | 网络不通。公司网络可能需要代理，或换个网络试试 |
| 能连上但回复特别慢 | 换个小模型试试（如 `deepseek-chat` 本身就比较快） |
| 回复到一半断了 | 大多是网络波动，点「重新生成」 |
| 窗口一团黑打不开 | 在项目文件夹执行 `npm start`，看命令行报的错，把错误发出来 |
| 脚本被禁用，提示 about_Execution_Policies | 在 PowerShell 里执行一次 `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned`，或者改用手动命令方式 |

**网络不顺时，先执行这三条命令再重试**（国内镜像，能解决绝大多数装不上的问题）：

```
npm config set registry https://registry.npmmirror.com
npm config set ELECTRON_MIRROR https://npmmirror.com/mirrors/electron/
npm install
```

**想看更详细的报错**：在项目文件夹的地址栏输入 `cmd` 回车，然后执行

```
set BARBARA_OPEN_DEVTOOLS=1
npm start
```

会额外弹出开发者工具窗口，里面 Console 一栏的错误信息最有用。

---

## 项目文件说明

```
Barbara\
├── package.json            项目说明（依赖、启动命令）
├── main.js                 主进程：开窗口、读写文件、调用模型接口
├── preload.js              安全桥梁：界面只能通过它调用主进程
├── renderer\
│   ├── index.html          界面骨架
│   ├── style.css           界面样式
│   └── js\                 界面逻辑（ES module，一拆多）
│       ├── main.js         入口：大部分功能还在这，会继续往外搬
│       ├── core\           常量、状态、DOM 引用、preload 桥、工具函数
│       ├── ui\             提示条、确认框、主题、Markdown、建 DOM 的小工具
│       └── data\           持久化等纯逻辑
├── tools\                  冒烟测试（npm run smoke），不属于应用本身
├── Start-Barbara.cmd       双击启动（推荐：自动绕过脚本执行策略）
├── Start-Barbara.ps1       启动器正文（**纯 ASCII，不要在里面写中文**）
├── messages-utf8.txt       启动器的中文提示词（UTF-8，可自由改文案）
├── Diagnose.ps1            出问题时运行它，生成 info-diagnose.txt
└── 使用说明.md             本文件
```

> 两个启动器作用完全一样，**任选一个**，不用都试。
> 改名（Cyrene → Barbara）之前留下的 `Start-Cyrene.cmd`、`Start-Cyrene.ps1`
> 已经用不到了，可以删掉。如果你手上还有 `启动 Cyrene.bat` / `启动 Cyrene.ps1` /
> `启动 Cyrene.cmd` / `诊断.ps1` 这类更早的旧文件，也可以一起清理。

> **为什么提示词要单独放一个文件？** 因为 Windows PowerShell 5.1 判断 `.ps1` 文件编码时
> 有时会误用系统 ANSI（中文系统是 GBK），导致脚本里的中文变成乱码、字符串引号错位，
> 报出一堆莫名其妙的「括号不匹配」。`Start-Barbara.ps1` 现在一个中文都没有，就再也不会有这个问题了。
> 想改提示文案，直接编辑 `messages-utf8.txt` 即可（**保存时务必选 UTF-8**）。

> 项目里原有的 `Cyrene-Setup-*.exe`、`latest.yml` 等文件是旧版本的安装包，和这套源码无关，想清理可以自行删除。

---

## 下一步可以加什么

按「性价比」从高到低排：

1. **加人设预设按钮** —— 设置里放几个常用角色，一键切换（工作量：小）
2. **导出对话为 Markdown 文件** —— 方便整理成笔记（小）
3. **对话搜索** —— 会话多了之后很有用（中）
4. **上传文档问答（RAG）** —— 让它读你的 PDF 再回答，需要引入向量检索（大）
5. **联网搜索** —— 需要再接一个搜索 API（中）
6. **打包成 exe 分发** —— 用 `electron-builder`，别人不用装 Node.js 就能用（中）
