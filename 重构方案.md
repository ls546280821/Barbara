# 重构方案：把 5000 行的 renderer.js 拆成模块

> 这份文档**只做体检和方案，没有改动任何代码**。
> 数据是静态分析工具跑出来的（词法遮罩后统计，不受注释/字符串干扰）。

---

## 一、结论

| 问题 | 答案 |
| --- | --- |
| 要不要拆？ | **要**，`renderer.js` 4939 行 / 185 个函数已经超出「靠滚动能维护」的范围 |
| 按"组件"拆？ | **不要**。没有框架、没有构建、HTML 是单个静态文件，硬套组件 = 给自己加一层模板/构建 |
| 拆成什么？ | **按功能拆模块（feature module）**，一个文件一块职责 |
| 需要构建工具吗？ | **不需要**。已实测 ES modules 在本项目 Electron 版本 + 当前 CSP 下可跑（见 §2.1） |
| 现在能直接拆到目标状态吗？ | **不能**。有 19 对分区互相调用，直接按分区切会切出一堆循环依赖（见 §3.3） |
| 最大风险 | **没有测试**。所以第 0 步是先做一个冒烟测试脚手架，再动手 |

**一句话**：方向对，但必须「先抽公共层 → 再拆功能」，而且要分 6 步走，每步都能单独回退。

---

## 二、五个已确认的事实（决定怎么拆）

### 2.1 ES modules 在 file:// 下可用（实测）

我写了个探针，在**这个 Electron 版本 + index.html 当前那条 CSP** 下加载真实页面：

```
default-src 'none'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:;
```

结果：`import`/`export` 正常、`import.meta` 正常、**preload 桥（22 个方法）照常可见**、控制台零报错。

> 结论：把 `<script src="renderer.js">` 换成 `<script type="module" src="js/main.js">` 就能拆，
> `npm start` 原地跑，不需要 webpack/vite。

### 2.2 dev 模式的自动刷新不受影响

`main.js:1321` 用的是 `fs.watch(renderer目录, { recursive: true })` —— **递归监听整个目录**。
所以文件挪进 `js/`、`css/` 子目录后，"保存即刷新"照旧，**不用改一行**。

### 2.3 代码其实已经分好组了，但**分区 ≠ 模块边界**

`renderer.js` 里已有 **27 个 `// ---` 分区横幅**，`main.js` 里 **9 个**。
但静态分析发现：**好几个被全项目调用的公共函数，物理上住在某个功能分区里面**：

| 函数 | 实际所在分区 | 被多少个不同分区调用 |
| --- | --- | --- |
| `showToast` | **角色（角色扮演）** | **16** |
| `confirmDialog` | **白天 / 夜间模式** | 7 |
| `persistConversations` | **白天 / 夜间模式** | 11 |
| `esc` | 极简 Markdown 渲染 | 全项目 |
| `worldbooks` / `worldbookById` / `convoWorldbookIds` / `matchWorldbookSection` | **角色（角色扮演）** | 5~6 |
| `scrollToBottom` | **角色（角色扮演）** | — |

这是**当前最大的结构问题**：如果直接按分区切，`记忆管理.js` 要弹个提示就得
`import { showToast } from './角色.js'` —— 循环依赖就是这么来的。

### 2.4 耦合密度

| 文件 | 行数 | 顶层函数 | 关键全局 |
| --- | --- | --- | --- |
| `renderer/renderer.js` | 4939 | 185 | `state.` 99 处、`el.` 348 处、`api.` 27 处 |
| `main.js` | 1786 | 57 | 20 个 IPC 通道（8 组） |
| `renderer/style.css` | 2121 | — | — |
| `renderer/index.html` | 762 | — | — |

`renderer.js` 前 210 行是事实上的「核心层」：14 个顶层声明
（`api`、`$`、`el`、`state` + 9 个状态变量），任何模块都要用。

### 2.5 fan-in 排名（谁才是真正的公共层）

被**不同分区**调用的次数排名：

```
16  showToast              14  now                    11  activeConvo
11  persistConversations    7  uid                     7  confirmDialog
 6  currentWorldbook        6  renderWorldbookPage     6  renderAll
 6  worldbooks              5  worldbookCharacters      5  worldbookById
 4  characterForConvo       4  showView                4  renderHeader
 4  createConvo             4  renderWorldbookChars     4  characters
```

前 6 个（`showToast`/`now`/`activeConvo`/`persistConversations`/`uid`/`confirmDialog`）
就是**必须最先抽出来的公共层**。

---

## 三、体检数据

### 3.1 分区规模（renderer.js，按行数）

| 行范围 | 行数 | 函数 | 分区 |
| --- | ---: | ---: | --- |
| 4338-4885 | 548 | 18 | 角色列表页 |
| 807-1225 | 419 | 8 | 渲染：会话列表、标题、消息 |
| 1805-2162 | 358 | 21 | 分段记忆摘要 |
| 2402-2713 | 312 | 6 | 发送与流式接收 |
| 3638-3908 | 271 | 2 | 事件绑定 |
| 2714-2980 | 267 | 14 | 设置弹窗 |
| 3167-3411 | 245 | 15 | 本书角色 |
| 2163-2401 | 239 | 9 | 记忆管理 UI |
| 3412-3637 | 226 | 8 | 世界书编辑器 |
| 4119-4337 | 219 | 12 | 进入世界：玩家角色 |
| 289-490 | 202 | 17 | 角色（角色扮演） |
| 1356-1548 | 193 | 15 | 状态面板（解析） |
| 2981-3166 | 186 | 11 | 世界书（数据访问） |
| 647-806 | 160 | 3 | 极简 Markdown 渲染 |
| 1624-1768 | 145 | 10 | 状态面板 UI |
| 1226-1355 | 130 | 5 | 会话管理 |
| 3909-4018 | 110 | 6 | 角色库 |
| 491-590 | 100 | 5 | 白天/夜间 + **confirmDialog + persistConversations** |
| 1549-1623 | 75 | 6 | 叙述模式 / GM 模式 |
| 4046-4118 | 73 | 3 | 世界书列表页 |
| 591-646 | 56 | 1 | 流式文字的重绘 |
| 4886-4940 | 55 | 1 | 启动 |
| 239-288 | 50 | 4 | 多模型：服务商 |
| 1769-1804 | 36 | 3 | 视角设置 UI |
| 4019-4045 | 27 | 3 | 主区域视图切换 |
| 223-238 | 16 | 3 | 工具 |
| 211-222 | 12 | 1 | 常量配置 |

### 3.2 main.js 分区

| 行范围 | 行数 | 函数 | 分区 |
| --- | ---: | ---: | --- |
| 670-1029 | 360 | 30 | 世界书 / Lorebook |
| 1408-1756 | 349 | 1 | IPC 注册（一个巨型函数） |
| 1030-1290 | 261 | 7 | HTTP 请求 |
| 240-470 | 231 | 9 | 服务商 / 模型 |
| 471-669 | 199 | 11 | 角色库（含 PNG/JSON 导入解析） |
| 113-239 | 127 | 8 | 本地存储（userData） |
| 1301-1407 | 107 | 7 | 开发模式 |
| 1757-1787 | 31 | 1 | 启动 |
| 1291-1300 | 10 | 3 | 窗口 |

20 个 IPC 通道，天然分 8 组：
`settings(3) models(1) conversations(3) characters(4) worldbooks(4) images(1) chat(2) util(2)`

### 3.3 循环依赖：19 对分区互相调用

直接按分区切文件，会产生循环 import 的分区对：

```
渲染：会话列表、标题、消息  ↔  会话管理 / 状态面板 UI / 分段记忆摘要 / 发送与流式接收
                             / 设置弹窗 / 进入世界 / 角色列表页        ← 一个中枢，双向 7 对
分段记忆摘要                ↔  记忆管理 UI
世界书编辑器                ↔  角色列表页 / 世界书列表页 / 角色（角色扮演）
本书角色                    ↔  世界书列表页 / 角色（角色扮演）
角色库                      ↔  角色列表页
主区域的视图切换            ↔  角色列表页
白天 / 夜间模式             ↔  设置弹窗
```

**根因有两个**：

1. `renderAll()`（在"渲染"分区）被 **6 个分区**调用 —— 它是"全量重绘"中枢，
   谁改完数据都喊它一声，于是它和所有视图互相依赖。
2. §2.3 说的：公共函数住在功能分区里，逼着别的功能去 import 它。

### 3.4 重复代码（结构相似度，已把标识符统一成 `ID` 后比较）

| 结构相似 | 原始相似 | 一对函数 |
| ---: | ---: | --- |
| **100%** | 25% | `renderWorldbookPage` ~ `renderCharacterPage` |
| 76% | 47% | `importCards` ~ `importWorldbooks` |
| 64% | 19% | `deleteSummarySegment` ~ `removeWorldbookCharacter` |
| 63% | 40% | `openPerspectiveModal` ~ `openMemoryModal` |
| 59% | 30% | `testConnection` ~ `fetchModels` |
| 54% | 10% | `formatPanelForPrompt` ~ `formatSummaryForPrompt` |
| 53% | 30% | `renderWorldbookChars` ~ `characterCard` |
| 53% | 31% | `removeWorldbookCharacter` ~ `deleteEntry` |
| 53% | 10% | `newWorldbook` ~ `startCharDraft` |
| 50% | 15% | `openMemoryModal` ~ `openWorldbookCharPicker` |

注意 `renderWorldbookPage` 和 `renderCharacterPage` **结构相似度 100%** ——
两个列表页是同一套逻辑抄了两遍（空状态切换、副标题计数、循环 append 卡片）。
这类"列表页骨架"和"卡片骨架"值得抽成小工具，**收益比拆目录还快**。

---

## 四、目标目录

```
main.js                      # 只剩：app 生命周期 + 窗口 + 装配（~80 行）
main/                        # 主进程模块（CommonJS，零构建）
  store.js                   #   userData 读写：settings / conversations / characters / worldbooks
  providers.js               #   服务商 + 模型列表
  characters.js              #   角色卡 + PNG/JSON 导入解析
  worldbooks.js              #   世界书 + 关键词匹配
  http.js                    #   大模型 HTTP（流式）
  window.js                  #   窗口 + 开发模式热重载
  ipc.js                     #   20 个 channel 的注册（每个只做参数转发）

renderer/
  index.html                 #   保持单文件（不要动，见 §7）
  css/                       #   可选，最后再做
    base.css  layout.css  chat.css  characters.css  worldbook.css  memory.css  settings.css
  js/
    main.js                  #   入口：init()
    core/                    #   ★ 第 0 层：谁都能 import，它不 import 任何业务
      config.js              #     CONFIG 常量
      state.js               #     全局 state（唯一的可变状态出口）
      dom.js                 #     $  +  el 大对象（150 个 DOM 引用）
      api.js                 #     window.barbara 的薄封装
      util.js                #     uid / now / activeConvo / esc / clamp...
    ui/                      #   ★ 第 1 层：通用界面件，不认识业务
      toast.js               #     showToast        （fan-in 16，必须最早搬）
      confirm.js             #     confirmDialog    （fan-in 7）
      theme.js               #     白天/夜间
      markdown.js            #     renderMarkdown / renderInline
      card.js                #     ★ 新抽：列表页骨架 + 卡片骨架（消除 §3.4 的 100% 重复）
    data/                    #   ★ 第 2 层：纯逻辑，不碰 DOM，最容易测
      providers.js           #     providers / currentEndpoint / ensureConvoEndpoint
      panel.js               #     extractPanelFromText / stripPanelLines / convoPanel...
      narration.js           #     叙述模式 / GM 规则文本
      memory.js              #     分段摘要：buildTranscript / generateSummary / maybeSummarize
      messages.js            #     buildApiMessages
    views/                   #   ★ 第 3 层：功能模块，一个文件一块
      conversations.js       #     会话列表 + 会话管理
      chat.js                #     消息渲染 + 发送 + 流式
      characterList.js       #     角色列表页
      characterEditor.js     #     角色编辑弹窗（含这一轮加的草稿逻辑）
      characterImport.js     #     导入角色卡 / 世界书
      worldbook.js           #     世界书弹窗 + 条目
      worldbookChars.js      #     本书角色（副本）
      worldbookList.js       #     世界书列表页
      player.js              #     进入世界前建角色
      panelUi.js             #     状态面板 UI
      memoryUi.js            #     记忆管理 UI
      perspectiveUi.js       #     视角设置 UI
      settings.js            #     设置弹窗
      refresh.js             #     ★ 新抽：renderAll 的替代品（见 §5.2）
    bindings.js              #   所有事件绑定（271 行，一个文件够了）
    init.js                  #   启动流程
```

**文件数量**：主进程 8 个 + 渲染层约 30 个。最大单文件预计 300~350 行。

---

## 五、两条铁律

### 5.1 依赖方向只能向下

```
core  ←  ui  ←  data  ←  views  ←  bindings / init
```

**下层绝对不能 import 上层**，同层之间不要互相 import。
现在做不到的地方（§3.3 那 19 对）靠 §5.2 的手段化解。

### 5.2 用「注册回调」代替互相 import

两个具体做法：

**① 用一个刷新总线替掉 `renderAll()`**

```js
// views/refresh.js —— 谁想被重绘就在自己模块里登记一下
const listeners = new Set();
export function onRefresh(fn) { listeners.add(fn); }
export function refreshAll(opts) { for (const fn of listeners) fn(opts); }
```

然后 `chat.js` 里登记 `renderMessages`、`characterList.js` 里登记 `renderCharacterPage`……
`记忆管理` 要重绘时只 `import { refreshAll } from './refresh.js'`，
**不再需要认识 chat / 角色 / 世界书**。§3.3 里 7 对循环当场消失。

**② 跨模块的"打开某个弹窗"用回调表**

`bindings.js` 是唯一允许"认识所有人"的地方 —— 它把所有模块的事件处理函数接起来。
视图之间要跳转（比如角色卡点「聊天」要新建会话），让 `bindings.js` 注入：

```js
// characterList.js
let handlers = { chatWith: () => {}, refresh: () => {} };
export function configureCharacterList(h) { handlers = { ...handlers, ...h }; }
```

---

## 六、迁移步骤（每步都可独立运行、独立回退）

> 每一步 = 一个 git commit，拆坏了 `git revert` 就回去。

### 第 0 步：冒烟测试脚手架 —— ✅ 已完成

项目**没有测试**（`package.json` 里没有 test 脚本）。现在补上了：

```bash
npm run smoke        # 通过 → 退出码 0；有断言失败 → 退出码 1
```

| 文件 | 作用 |
| --- | --- |
| `tools/smoke-test.js` | Electron 外壳：开一个**不显示**的窗口，加载真实 `renderer/index.html`，把 20 个 IPC 全换成内存里的假后端 |
| `tools/smoke-renderer.js` | 跑在页面里的场景脚本：点真实按钮、读真实 DOM、断言结果 |

**它不碰你的数据**：所有读写都留在内存里，随便跑都不会弄坏你的角色和会话。

当前覆盖 **10 个场景 / 37 条断言**：

1. 启动（主界面渲染、没有掉进「启动失败」兜底页、自动建会话）
2. 主题切换 + 落盘
3. 设置弹窗开关
4. **角色库：新建角色**（草稿不落盘 → 保存后才落盘 → 标题/按钮状态正确）
5. **角色库：放弃新建**（弹确认框 → 放弃后什么都没留下）
6. **角色库：编辑已有角色**（是更新不是新增）
7. **聊天**：发一条 → 真的走一遍 `chat:chunk` 流式通道 → 回复渲染、没有错误气泡、没有卡在流式态
8. **世界书：新建条目** → 落盘
9. **世界书：本书角色** → 副本落盘 + **z-index 回归断言**（编辑器不能被世界书弹窗盖住）
10. **进入世界：玩家角色弹窗**开关

**关键设计**：测试只「点按钮 + 读 DOM」，**绝不调用内部函数**。
因为改成 ES module 之后模块作用域里的变量就不再是全局的，
那种写法会立刻失效；而「点按钮 + 读 DOM」能扛住整个重构。

**已验证它不是橡皮图章**：把 `#chars-modal` 的 `z-index` 改回 50（也就是重新引入第 9 条的 bug），
测试立刻报 `✗ 编辑器没有被世界书弹窗盖住 ← 最上层是 worldbooks` 并以退出码 1 结束。

有了它，后面每一步都能自动验一遍。


### 第 1 步：切 ESM —— ✅ 已完成

- `renderer/renderer.js` → `renderer/js/main.js`（`git mv`，内容一字不改）
- `index.html` 的 `<script src="renderer.js">` → `<script type="module" src="js/main.js">`

验证：冒烟测试 37/37 通过、控制台 0 报错。回退就是改回一行。

### 第 2 步：抽 core —— ✅ 已完成

`renderer/js/core/`（5 个文件，共 253 行）：

| 文件 | 内容 | 为什么先搬它 |
| --- | --- | --- |
| `config.js` | `CONFIG` | 不依赖任何东西 |
| `api.js` | `window.barbara` 桥 | 不依赖任何东西 |
| `state.js` | 全局 `state` 对象 | 谁都要读它 |
| `dom.js` | `$` + `el`（150 个 DOM 引用） | 引用最密（348 处） |
| `util.js` | `uid` / `now` / `activeConvo` | **fan-in 最高**（`now` 14 个分区在用） |

`main.js` 从 4939 行降到 4487 行。

### 第 3 步：抽 ui + 持久化 —— ✅ 已完成

| 文件 | 内容 | 搬走的关键 |
| --- | --- | --- |
| `ui/toast.js` | `showToast` | **fan-in 16**，原来住在「角色」分区里 |
| `ui/confirm.js` | `confirmDialog` | 原来住在「白天/夜间模式」分区里 |
| `ui/theme.js` | `currentTheme` / `applyTheme` / `toggleTheme` | |
| `ui/markdown.js` | `esc` / `renderInline` / `renderMarkdown` | 完全不依赖别的模块，可单独测 |
| `data/persist.js` | `persistConversations` | 原来也在主题分区里 |

两个小技巧，让这一步不用改动任何函数体：

- `toastTimer` / `saveTimer` 原来挂在全局，实际只有 `showToast` 和
  `persistConversations` 用 —— 直接变成各自模块里的私有变量（**不能**把 `let`
  搬进 core 再 import：ES module 的 import 绑定是只读的，别的模块赋值会报错）。
- 编辑器那一堆零散状态（`editingCharacterId` / `charDraft` / `currentView` …）
  **故意先留在 main.js**：它们要被多个模块读写，得先改成对象属性才能跨模块赋值，
  那是第 5 步搬 views 时才需要做的事。

验证：冒烟测试 37/37 通过、控制台 0 报错。§2.3 那个「公共函数住在功能分区里」的问题到此消失。

### 第 4 步：抽 data（纯逻辑，最好搬）

`panel`（15 个函数，0 处 `state`/`el`）、`narration`（6 个，0 处）、`memory` 摘要（21 个，0 处）、
`buildApiMessages`、`providers`。这几块**一行都不用改**，剪过去加 `export` 就行，
而且它们内部几乎没有 DOM 依赖 —— 顺手还能给它们补几个单元测试。

### 第 5 步：拆 views（一次一个，从依赖少的开始）

顺序：`player` → `memoryUi` → `perspectiveUi` → `panelUi` → `worldbookList` →
`worldbook` → `worldbookChars` → `characterImport` → `settings` →
`characterEditor`/`characterList` → **`chat` 放最后**（它和谁都有关）。

每拆一个：**先引入 `refresh.js` 把 `renderAll` 的调用换掉**，再搬文件。

### 第 6 步：`bindings.js` + `main.js` 入口

### 第 7 步（可以完全独立、随时做）：拆 `main.js`

主进程是纯 Node + CommonJS，**没有 DOM、没有全局状态**，57 个函数 / 20 个 IPC 分组清晰。
风险比渲染层低得多，**完全可以先做这一步练手**。
验证方式：`npm start` 能开窗、能存设置、能导入卡片、能聊天。

### 第 8 步（可选）：拆 CSS

`@import` 即可（CSP `style-src 'self'` 允许），但 CSS 没有作用域，收益最小，放最后。

---

## 七、明确不要做的四件事

1. **不要引入 webpack/vite + React/Vue。**
   这个项目的卖点是"没有依赖、改完存盘就刷新、普通人能读懂改"（`使用说明.md` 就是这么写的）。
   加构建 = 用"构建配置 + 框架心智负担"换掉"5000 行文件"，不划算。
2. **不要把 `index.html` 按功能拆成多个 HTML / 模板。**
   762 行集中在一个文件里，用 `id` 和模块对应，是可读的；
   拆了就必须引入模板机制，得不偿失。JS/CSS 拆，HTML 不拆。
3. **不要按"组件"拆**（avatar.js / modal.js 这种）。
   这里没有组件运行时，按 UI 碎片拆只会产生一堆互相 import 的碎片。
   按**功能**拆：一个功能能独立说清楚"它是干嘛的"。
4. **不要一次性大重构。**
   没有测试兜底 + 一个能跑的产品 = 分步走 + 每步冒烟 + 每步可 revert。

---

## 八、工作量与建议

| 阶段 | 内容 | 预估 | 风险 |
| --- | --- | ---: | --- |
| 第 0 步 | 冒烟测试脚手架 | ~~2~3 小时~~ **已完成** | 无（不碰应用代码） |
| 第 1~3 步 | ESM + core + ui | 半天 | 低 |
| 第 4 步 | data（纯逻辑） | 2~3 小时 | 低 |
| 第 5 步 | views 逐个拆 | 1 天 | **中**（要处理 §3.3 的循环） |
| 第 6 步 | bindings + 入口 | 1 小时 | 低 |
| 第 7 步 | main.js | 2~3 小时 | 低 |
| 第 8 步 | CSS | 1 小时 | 极低 |

**合计约 2 个工作日。**

如果不想一次投入这么多，**性价比最高的三个小改动**（合计半天，可单独做）：

1. **抽 `ui/card.js`**：消掉 `renderWorldbookPage` / `renderCharacterPage` 那对 **100% 结构重复**，
   以及 `renderWorldbookChars` / `characterCard`、`openPerspectiveModal` / `openMemoryModal` 等 10 对。
2. **把 `showToast` / `confirmDialog` / `esc` / `persistConversations` 挪到公共位置**：
   它们现在住在"角色"和"主题"分区里，是全项目最高 fan-in 的函数，位置错了会持续误导后来的人。
3. **`renderAll()` 换成一个显式的 `refresh()`**：它是 19 对循环依赖的主要来源。

**我的建议**：先做第 0 步（冒烟测试），再做第 1~3 步（core + ui），
这三步做完你会发现"找东西"已经明显好转，而风险几乎为零；
views 那一大块（第 5 步）可以等真的要继续加功能时再动。

---

*本文档由静态分析生成：解析 4939 行 renderer.js 与 1786 行 main.js，
建立「分区 → 函数 → 跨区调用」图（185 + 57 个函数、约 420 条调用边），
并对函数体做原始/结构双重相似度比对。*
