# 2026-09-23

## 调研：该不该现在按《重构方案.md》动手重构

星宝问「根据文档重构项目，还是说目前还不需要重构，因为还有些功能没做」。
只做了调研和判断，**没有改任何代码**。

### 复核结果：文档数据没过期，可以放心用

文档 `重构方案.md` 的 `2026-09` 复核（commit `b7eae0a`）跟当前代码完全对得上：

| 项 | 文档 §九 | 实测 |
| --- | --- | --- |
| `renderer/js/main.js` | 7902 | 7902 |
| `main.js` | 1815 | 1815 |
| `renderer/js/core/` | 5 个 | 5 个 |
| `renderer/js/ui/` | 5 个 | 5 个 |
| `renderer/js/data/` | 2 个 | 2 个 |
| `renderer/js/views/` | 不存在 | 不存在 |
| `renderAll(` 调用 | 22 次 | 23 处（含定义，即 22 次调用） |
| `importCards` / `importWorldbooks` | 7717 / 7779 | 7717 / 7779，仍未合并 |

工作区干净，全部在 git 里。

### 关键判断：「还有功能没做」只对一半成立

- `使用说明.md` §「下一步可以加什么」那 5 条（人设预设 / 对话搜索 / 文档问答 RAG /
  联网搜索 / 打包 exe）是**可选新增想法**，不是「做了一半必须收尾」的半成品。
  项目里没有正在开发的半成品功能。
- 所以「功能没做完」这个理由，**只对 `views/` 全面拆分（第 5 步）成立**：
  那是中风险、要一次搬十几个文件的重活，每加一个新功能就得多搬一块，还会「搬两次」。
- 但文档 §八 那三件是**零/低风险、与功能进度无关**的，不做也迟早要做，且每加功能收益只增不减。

### 给出的建议（分两批）

**现在就做：**
1. 合并 `importCards` / `importWorldbooks`（7717 / 7779）—— 纯赚，零依赖变更，顺带修「只有一边改按钮文字」的不一致
2. 抽 `data/` 那 5 个纯逻辑模块（`providers` / `panel` / `narration` / `memory` / `messages`）—— 不碰 DOM，剪过去加 `export` 即可，搬完能补单元测试；同时是 views 的依赖，等于铺路
3. 做 `refresh` 总线 —— 第 5 步的死结前置，做了之后新加视图功能也能直接用

**缓做：** 第 5 步 views 拆分（7902 行的主战场）。
触发它该做的信号：功能收敛了 / 准备打包分发（`electron-builder`）/ 改 `main.js` 开始吃力。

已知约束：`data/panel.js` 将来搬的时候要把 `main/panel-fields.js` 这个共用依赖一起带过去。

---

## 重构第一批落地（data 层 4/5 + 刷新总线）—— 已实施

星宝决定：「先做 1（合并导入函数），再做 2（抽 data 模块），最后 3（refresh 总线）」，
并明确「**后面只是优化，没什么大功能要加**」→ 意味着 views 拆分也该排上日程了。

产出 6 个 commit（`1fd461f` … `9978744`），每步都跑过冒烟测试（431/431）：

| commit | 内容 |
| --- | --- |
| `1fd461f` | 合并 `importCards` / `importWorldbooks` → `pickImportFiles()` |
| `04a8085` | 抽 `data/providers.js` + `data/panel.js`，顺带新增 `core/panel-fields.js` |
| `a7c65bd` | 抽 `data/narration.js`（零依赖） |
| `9292743` | 抽 `data/memory.js`（调度部分留在 main.js） |
| `ef760bc` | 引入 `views/refresh.js` 刷新总线 |
| `9978744` | 文档同步（重构方案 / README / CHANGELOG） |

`renderer/js/main.js`：**7902 → 7169 行**（首次净减少）。

### 关键判断：`data/messages.js` 推迟了（复核后改的）

原计划把 `buildApiMessages` 搬进 `data/messages.js`。实测发现它需要**连带迁移 14 处**
代码：`characters` / `characterById` / `characterForConvo` / `worldbooks` /
`worldbookById` / `convoWorldbookIds` / `worldbookCharacters` / `effectiveWorldbookIds` /
`recursiveDepthSetting` / `userName` / `applyMacros` / `parseExampleDialogue` /
`messageImages` / `convoPlayer` / `convoUserName` / `speakerName`（外加几个常量）。

这些函数**同时被界面大量调用**，本质是「共享数据层」而不是纯逻辑，
硬塞进 `data/` 只会制造新耦合。正确顺序：先有 `data/library.js`（角色库 + 世界书读取器），
而那应该和 `views/` 拆分一起规划。收益（可单测）也已被冒烟测试的宿主侧断言覆盖大半。

### 下次接手：第 5 步（拆 views）的前置已经解开，可以直接开始

拆分顺序：`player` → `memoryUi` → `perspectiveUi` → `panelUi` → `worldbookList` →
`worldbook` → `worldbookChars` → `characterImport` → `settings` →
`characterEditor`/`characterList` → **`chat` 最后**（它和谁都有关）。

每拆一个：把 `registerRefreshListeners()`（在 main.js 里）那一行登记语句搬进新模块；
模块内要重绘就 `import { refreshAll } from './refresh.js'`，**不要 import 别的视图模块**。

### 本项目开发要点（都是这轮踩出来的）

**冒烟测试怎么跑**（本机环境有坑，缺一不可）：
```bash
cd E:/工作/Barbara
unset ELECTRON_RUN_AS_NODE     # ← 环境里默认带着这个变量，electron 会退化成纯 node
npx electron tools/smoke-test.js --no-sandbox
```
- 通过：退出码 0，末尾「断言：431/431 通过」
- 断言失败：标 ✗，退出码 1
- **模块加载失败**：表现为「90 秒超时」退出码 2（看门狗在 `tools/smoke-test.js:61`）

**ES module 的坑**：`import` 一个没有 `export` 的名字 → **整个模块图不执行**。
页面 HTML 照常显示、控制台只有一行 SyntaxError，但所有 JS 都不跑 ——
冒烟测试的表现是「90 秒超时」而不是失败断言，日志里什么都没有。
排查：写个小脚本用 electron 加载 `renderer/index.html`，监听 `console-message` 打印。
（这次是漏了 `MAX_PANEL_FIELDS` 的 `export`，就这么找到的。）

**搬大块代码的工具**：删/搬几百行时别手写超长 diff。写个 Node 脚本按
「锚点 + 内容校验」删除 —— 校验「待删区间必须包含预期的标识符，否则拒绝执行」，
比肉眼数行号可靠得多，而且行号一直在漂、不能硬编码。

**语法自查**（几秒，比跑冒烟快得多）：
`new vm.SourceTextModule(fs.readFileSync(f, 'utf8'))` —— 只解析不执行，
能抓语法错（但抓不到「import 不存在的导出」）。需要 `node --experimental-vm-modules`。

---

## 重构第二批：views 开拆（library 地基 + player）—— 已实施

星宝选「接着拆，小步试水」。产出 3 个 commit：

| commit | 内容 |
| --- | --- |
| `3a9b4f5` | 抽 `data/library.js`（角色库 / 世界书只读访问器，6 个函数） |
| `300e07d` | 拆出 `views/player.js`（进入世界的玩家角色弹窗） |
| `5d1dc1f` | 文档同步（重构方案 / README / CHANGELOG） |

`renderer/js/main.js`：**7169 → 7037 行**；`data/` 7 个文件；`views/` 2 个文件。

### 关键修正：拆 views 的真正前置是 `data/library.js`

文档判断「`player` 依赖最少，所以排第一个拆」—— **实测站不住**。它依赖 9 个还住在
`main.js` 里的函数，其中 6 个是读取器：`characters` / `characterById` / `worldbooks` /
`worldbookById` / `worldbookCharacters` / `characterAttrs`。这六个都是 1~3 行、
只依赖 `state` + 那份共享归一化，所以先抽成了 `data/library.js`。
**不抽出来的话，每个视图模块都要把这批读取器注入一遍，拆到第三个就开始重复。**
→ 第 5 步的实际顺序修订为：**先 `data/library.js`，再拆 views。**

### `player` 只搬了弹窗，动作编排是故意留下的

`startWorldPlay` / `generateWorldOpening` 留在 `main.js`。原因：要调 `createConvo` /
`seedIdentity` / `seedPanelFromCharacters` / `applyMacros` / `showView` /
`worldbookCast` / `renderMessages` —— 其中 `renderMessages` 是聊天视图的函数，
views 层调它 = 两个视图互相 import，违反「依赖方向只能向下」。
**归位顺序**：先让 `createConvo` → `data/conversations.js`、`seed*` → `data/panel.js`，
再搬这两个动作。（`seed*` 其实本来就是纯 panel 逻辑，只依赖 `characterAttrs` +
`appendPanelFields`。）

`playingBookId` 跟着搬走了，`main.js` 通过 `getPlayingBook()` 取目标。
**`player.js` 不登记刷新总线** —— 弹窗是「打开时按需重画」，不参与全局重绘。
（上面第 84 行那条「每拆一个都要登记」对它不适用，别照抄。）

### 新踩的坑：并行编辑**同一个文件**，只有一处能落盘

一次发多个编辑到同一个文件时，只有一个生效、其余静默丢失 —— **每次调用都回报成功**。
这次的症状是冒烟测试报 `ReferenceError: closePlayerModal is not defined`，
因为加 `import` 的那次编辑被同批的另一次覆盖了。
**同一个文件的改动必须一条一条发**（改不同文件时并行没问题）。

### 下一个：`memoryUi`

顺序不变：`memoryUi` → `perspectiveUi` → `panelUi` → `worldbookList` → `worldbook` →
`worldbookChars` → `characterImport` → `settings` → `characterEditor`/`characterList` →
`chat` 最后。

---

## 重构第三批：views 拆掉 4 个功能模块 —— 已实施

星宝说「继续」。产出 5 个 commit：

| commit | 内容 |
| --- | --- |
| `a0721c1` | 拆出 `views/memoryUi.js`（记忆管理 + 存档点，508 行） |
| `7d914f1` | 拆出 `views/header.js`（对话头部，102 行） |
| `967ef11` | 拆出 `views/perspectiveUi.js`（视角设置，78 行） |
| `321b237` | 文档同步 |
| `26e0dd3` | `tools/diag-page.js` 固化 + `npm run diag` |

`renderer/js/main.js`：**7169 → 6476 行**；`views/` 5 个文件。

### 顺序又被现实修正：`header` 得先拆

原计划这轮拆 `memoryUi` → `perspectiveUi`。实测**两个都被 `renderHeader()` 卡住**：
- `summarizeNow`（记忆的手动压缩按钮）要调它
- `applyPerspectiveFromForm`（视角设置的 change 处理）要调它

header 不独立出去，搬这两个模块就会造成 views → main 的反向依赖。
所以中间插了一刀：先拆 `views/header.js`。

**关键认识**：header 只依赖数据层、不认识任何视图 → 别的视图可以**单向** import 它。
「依赖方向只能向下」这条铁律防的是**循环**，不是一切跨视图调用。
（已写进 `views/header.js` / `views/perspectiveUi.js` 的文件头注释。）

### 记忆和存档点必须住同一个文件

两者物理上共用 `#memory-modal`（上面摘要列表、下面存档点）：
`renderMemoryModal` 调 `renderCheckpoints` 画下半截，存档点的存/读/删又回头调
`renderMemoryModal` 重画整个弹窗 —— 双向调用。拆成两个模块就是两个视图互相 import。
所以合成 `memoryUi.js`（508 行）。「每块 300~350 行」在"不许循环"面前让位。

### 这轮沉淀的三条做法（下次直接照做）

1. **接线方式**：`registerRefreshListeners()` 里改成调用各模块的 `initXxx()`，
   **登记留在原位** → 绘制顺序和以前完全一致。`initXxx()` 兼做「绑事件 + 登记重绘」；
   只绑事件不登记重绘的（`perspectiveUi`）在 `init()` 里单独调用。
2. **沉到 data 层的判断标准**：某个东西如果**入口和视图两头都要用**，就沉到 `data/`，
   谁都不用 import 谁。这轮这么做的是：`characterForConvo` / `convoWorldbookIds`
   → `data/library.js`；`summarizingConvos` / `summaryFailures` / `charNameForSummary`
   → `data/memory.js`。
3. **`main.js` 留在入口的是「动作编排」**：凡是要同时改数据 + 刷新别的视图的动作
   （`startWorldPlay` / `maybeSummarize` / `summarizeNow`），留在入口层 ——
   入口 import 视图是合法的，反过来不行。

### 每刀的固定流程（已验证 4 次，稳定）

1. 摸边界：grep 目标函数 + 它的**全部调用点** + 它依赖谁
2. 判断跨视图调用（最容易漏的一步）→ 要么先拆那个依赖，要么留在 main.js
3. 写新模块（原注释一并带过去）
4. 用锚点脚本删 main.js 里的定义（几百行别手写 diff）
5. 改 import + 接线（**同一文件的编辑一条一条发**，见第二批那个坑）
6. `npm run diag`（3 秒，只验模块加载）→ 再跑完整冒烟（431/431）
7. 单独 commit

### 下一个：`panelUi`（状态面板）

仍留在 `main.js` 的跨层动作（等依赖就位）：
`startWorldPlay` / `generateWorldOpening` / `maybeSummarize` / `summarizeNow`。

---

## 重构第四批：拆分状态面板（panelUi）—— 已实施

星宝说「继续」。产出 3 个 commit：

| commit | 内容 |
| --- | --- |
| `24ccb64` | 把四个「种字段」写入口沉到 `data/panel.js` |
| `19861b9` | 拆出 `views/panelUi.js`（状态面板，299 行） |
| `19b4c3b` | 文档同步 |

`renderer/js/main.js`：**6476 → 6127 行**；`views/` 7 个文件（6 个功能模块 + 总线）；
`data/panel.js` 565 行。

### 新模式一：写入口也属于数据层（先沉 data，再拆视图）

`appendPanelFields` / `seedPanelFromCharacters` / `seedIdentity` / `syncPlayerNameFromPanel`
原本散在 `main.js` 的角色表单旁边（5913~6019），看着像面板 UI 的一部分。
实际被**入口层和聊天流程两头调用**（角色卡绑定、进世界开新会话、每轮回复后同步
玩家名）—— 是数据层的写入口。
**判断依据不是「读还是写」，而是「谁要用」。** 先沉 data 层，再做视图；
否则聊天流程得反过来 import 视图模块。

### 新模式二：视图需要入口层动作 → 注入

面板的「剧情选项」点下去 = 发一条消息（`pickOption`：动输入框、建议条、发送流程），
是入口层的编排，搬不进视图。改成 `initPanelUi({ pickOption })` 注入。
反方向（`main.js` import `renderPanel`）合法 —— 入口在链尾，可以认识所有人。
（本批第一次用注入；`header.js` 那种「只依赖数据层」的可以直接 import。）

### 新验证法：diff 两次完整日志

测试的「写操作记录」从 88 变 87。**diff 改前改后两份完整日志**：431 条断言、
30 个场景输出**逐字一致**，只有该计数不同，磁盘状态校验也一样。
→ 是 `persistConversations` 的 `clearTimeout` + `setTimeout` 防抖把两次重复落盘
合并了（多一个模块、同步段短几毫秒，就落进同一窗口）。**结论：不是行为变化。**
方法本身值得记：怀疑拆分引入行为差异时，diff 完整日志能证明「什么都没变」，
比盯断言数可靠。

### 下一个：`worldbookList`（世界书列表页）

剩余顺序：`worldbookList` → `worldbook` → `worldbookChars` → `characterImport` →
`settings` → `characterEditor`/`characterList` → **`chat` 最后**。

---

## 重构第五批：拆分世界书列表页（worldbookList）—— 已实施

星宝说「继续」。产出 2 个 commit：

| commit | 内容 |
| --- | --- |
| `0ff00d9` | 拆出 `views/worldbookList.js`（世界书列表页，69 行） |
| `b25c134` | 文档同步 |

`renderer/js/main.js`：**6127 → 6099 行**；`views/` 8 个文件（7 个功能模块 + 总线）。

**本批验证得最干净**：改前改后两份冒烟日志 `diff` **零行** ——
431 条断言、30 个场景输出逐字一致，写操作计数也稳定在 87。纯搬移，零行为差异。

### 判断细化：同一个卡片上两个按钮，处理方式不同

分界线是**会不会造成循环**，不是「按钮长得像不像」：

- **「编辑」→ 注入**。打开编辑器要设 `editingWorldbookId`，那是**编辑器弹窗**的状态
  （只有 `openWorldbooksModal` / `currentWorldbook` / `selectWorldbook` 在读）。
  列表页直接调就得 import 还没拆的编辑器 → `initWorldbookList({ openEditor })`。
- **「游玩」→ 直接单向 import** `./player.js` 的 `openPlayerModal`。
  `player.js` 只依赖 core + data、不认识任何视图，不构成循环，合法。

→ 三档判断已整合进 skill：**数据/写入口沉 data 层；只依赖 core/data 的展示层可单向
import；入口层动作才注入。**

### 公共障碍的另一种处理：拆完导出，调用点原样保留

`renderWorldbookPage` 被 `main.js` 里**十余处**调用（改名 / 删除 / 新建 / 导入后重绘），
是后续 `worldbook` / `worldbookChars` / `characterImport` 的共同依赖。
做法：搬到新模块后**导出**，`main.js` import 它、原调用点一行不动。
因为新模块不认识任何视图，后面那些模块拆出去时可以照样 import 它，不会成环。
（另一半是 `refreshLibraryPage()` —— 它要读 `currentView`，属于入口层分发，留在 main.js。）

### 下一个：`worldbook`（世界书编辑器弹窗）

注意它是本批留下的「大块」：`openWorldbooksModal` / `selectWorldbook` / `newWorldbook` /
`deleteWorldbook` / `previewWorldbook` / `stashWorldbookName` / `stashEntryForm` /
`worldbookPayload` / `exportWorldbook` 都在 3500~4100 一带，且 `editingWorldbookId`
要被多处读写 —— 拆之前先摸清它和 `worldbookChars`（本书角色）是不是共用容器
（很可能是，参考第三批 memoryUi 的教训）。

---

## 第六批：拆 `worldbook`（世界书编辑器弹窗）—— 751 行，main.js 6099 → 5363

### 原计划的 `worldbookChars.js` 不该单独存在

`selectWorldbook`（选书）要调 `renderWorldbookChars`（画本书角色那一排），
「移除副本」又要回头重画整个列表 —— **双向调用**，拆两个模块就是互相 import。
合并成 `views/worldbook.js`（751 行，目前最大的视图模块）。
和第三批「记忆 + 存档点共用 `#memory-modal`」是同一类问题，判据仍是**不许循环 > 行数目标**。

### 剥出六块「两头都要用」的

| 沉到 | 什么 | 两头是谁 |
| --- | --- | --- |
| `data/library.js` | `worldbookPayload` | 角色卡导出的 `character_book` vs 编辑器「导出本书」 |
| `data/library.js` | `newWorldbookCharId` | 编辑器「加入副本」vs 角色编辑器（世界书作用域）起草角色 |
| `data/library.js` | `WORLDBOOK_SCAN_DEPTH` / `recursiveDepthSetting` | 请求组装 `matchWorldbookSection` vs 编辑器「预览命中」 |
| `data/persist.js` | `persistLibrary` + `worldbooksLoaded`（含 `markWorldbooksLoaded` / `areWorldbooksLoaded`） | 世界书编辑器 / 角色编辑器 / 导入 三头都在写盘 |
| `data/export.js`（新） | `saveExport` | 角色卡 / 对话 / 世界书 三条导出链路的收尾 |
| `core/util.js` | `safeFileName` | 同上 |

> **`saveExport` 为什么放 `data/` 而不是 `core/`**：它要用 `showToast`（ui 层），
> 而 `core/` 目前**不** import `ui/`（分层是 core ← ui ← data ← views ← 入口）。
> 判据是依赖方向，不是「它看起来像不像底层工具」。

### 角色编辑器交互用注入（五个动作）

`initWorldbook({ importBooks, stashDraft, openInBook, draftInBook, releaseScope })`。
`charEditorScope` / `editingCharacterId` 这对共享状态**故意留着没沉** ——
它真正该进数据层，但要动 30+ 处调用点，那是「拆 `characterEditor`」那一刀的事。
**混在这一刀里就不是「只搬移」了。**

### 踩坑：删块时连「留着的逻辑」一起删掉了，靠 import 计数抓回来

删 ESC 分支时我按锚点把整段

```js
if (!el.wbPicker.modal.classList.contains('hidden')) { closeWorldbookCharPicker(); return; }
```

删了 —— 但这段逻辑**本该保留**（函数搬走了，调用点要改成 import 的那个）。
漏掉后 ESC 不再能关角色选择框，而**冒烟测试不会发现**（它不测 ESC 链路）。

抓到它的办法：拆完后逐个统计 `import` 进来的名字在入口文件里的出现次数，
**次数 == 1（只有 import 那一行）就说明没人调用**：

```bash
for n in initWorldbook renderWorldbookChars closeWorldbookCharPicker ...; do
  echo "$n: $(grep -c "\b$n\b" renderer/js/main.js)"
done
```

→ 已补进 skill 的验证步骤。

### 本批验证

- 431/431 断言全绿、控制台 0 报错
- 改前改后日志 diff **只有一行**：写操作 88 → 87（`persistConversations` 防抖把两次
  调用并进同一个 350ms 窗口；前几批出现过同一个现象，断言文字逐字一致）
- diff 的做法：先存新日志 → `git stash push -u` → 跑旧版存盘 → `git stash pop` → `diff`

### 下一个

`characterImport`（`importWorldbooks` / `pickImportFiles` 那一族，跨角色库编排）
→ `settings` → `characterEditor` / `characterList`（最大的，且要顺手把
`charEditorScope` / `editingCharacterId` 沉数据层）→ 最后 `chat`。

---

## 第七批：拆 `settings`（设置弹窗）—— 733 行，main.js 5363 → 4690

到现在**风险最低的一刀**：设置区是连续的 637 行，且**不被任何模块引用**
（拆前 grep 过 `views/` `data/` `ui/`，一处都没有）。改前改后日志 **diff 零差异**。

### 接线

注入两个入口层动作（都是「保存后要重绘、但重绘对象属于入口层」）：
`refreshModelSwitch`（右上角切换器）、`afterSettingsSave`（切换器 + 消息列表）。
导出 `openSettings`（发消息前没配模型、点「配图」没配生图时要弹）和 `closeSettings`。
`renderHeader` 直接 import —— header 只依赖 core/data，单向合法。

### ⚠️ 没搬的那条：Esc 关设置窗（本轮新判断）

入口层有个全局 Esc 处理器，按**固定顺序**判断关哪个弹窗：
确认框 → 玩家弹窗 → 角色选择 → 角色编辑 → **设置** → 外观。
把「设置」那条搬进 `initSettings` 会变成**第二个监听器** ——
两个弹窗同时开着时，原来只关最上面一个，搬完会两个一起关。

**判据：这段代码是「某个视图的实现」还是「一条有序链上的一环」？**
后者属于入口层。和上一批 `closeWorldbookCharPicker` 是同一个坑的两面
（上次是删过头，这次是搬过头）。

### 踩坑 A：生成脚本三个占位符只换了两个

我用「模板 + 占位符」组装新模块（`@@BIND1@@` / `@@BIND2@@` / `@@BODY@@`），
脚本里只 replace 了前两个，`@@BODY@@` 原样留在文件里 → **语法错误 → 整个模块图不执行**。

排查过程值得记：`node --check` 居然**没报错**（它对这个文件的解析宽松），
是 chromium 的 console 报了 `Uncaught SyntaxError ... <settings.js:100>` 才定位到。
（第一次 diag 跑了但被 GPU 报错淹了，第二遍才发现。）

**教训：用占位符组装文件，交付前必须校验「残留占位符 == 0」+「关键内容只出现一次」。**
另：修复时我把 `@@BODY@@` 换成 body，结果**重复了一份**（原文件已经是 head+body），
733 → 1367 行才发现 —— 所以「校验重复」和「校验残留」一样重要。

### 踩坑 B：环境坏了，看着像代码坏了

跑验证时 `npm run smoke` 报「页面加载失败：ERR_FAILED (-2)」，
**用改动前的版本（git stash）跑也一样** → 这就是「不是代码问题」最快的判定手段。

真因在日志深处：`FATAL: GPU process isn't usable. Goodbye.` ——
GPU 进程反复崩溃把主进程一起带走了。修法两处：
- `package.json` 的 `smoke`/`diag` 加 `--no-sandbox`
- 两个工具里加 `app.disableHardwareAcceleration()`

试过但**没用**的：清 `%APPDATA%\Electron\GPUCache`（+Dawn 缓存）、
`--use-angle=swiftshader`、`--disable-gpu` 单独用、杀残留 electron 进程。

### 下一个

`characterImport`（注意它「实体」部分早已沉完：解析在 `data/library-reissue.js`、
落盘在 `data/persist.js`，剩两个导入函数是跨视图编排，本就该留入口层）
→ `characterList`（卡上的「编辑 / 删除」要调角色编辑器，用注入解开，不必合并）
→ `characterEditor`（最大的一块，顺手把 `charEditorScope` / `editingCharacterId`
沉数据层）→ 最后 `chat`。

## 第八批：拆 `appearance`（外观弹窗）—— 214 行，main.js 4690 → 4494

`views/appearance.js`：白天 / 夜间的配色与气泡样式。
`initAppearance()` 注入；导出 `applyChatAppearance()`（`init()` 启动时按存下来的设置上色一次）
和 `closeAppearanceModal()`（入口层 Esc 链上的一环，同第七批 `closeSettings`）。

## 第九批：沉 chat 的四块数据地基 —— 只动 `data/`，main.js 4494 → 3867

| 文件 | 行数 |
| --- | ---: |
| `data/messages.js`（宏展开 / 示例对话解析 / 图片消息 / `buildApiMessages`）| 289 |
| `data/suggestions.js`（解析 / 指令拼装 / `syncConvoOptions`）| 167 |
| `data/cast.js`（`convoPlayer` / `convoUserName` / `speakerName` / `worldbookCast` / `matchWorldbookSection`）| 155 |
| `data/rag.js`（`recallSection` / `formatRagSection`）| 83 |

**判据「两头都要用」就沉下去**：`convoUserName()` 消息渲染要它（显示名字）、
组装提示词也要它（填 `{{user}}` 宏）—— 留任何一边另一边都得反过来 import。
注意 `cast.js` 顺带装了世界书命中与召回（全在「谁在说话」这个上下文里）。

这批判序在 `chat` 之前，是**为了后面那一刀能「只接线不重构」**。事后证明是对的：
第十三批是最大的一刀却拿到 diff 零行。

## 第十批：拆流式渲染三件套 —— main.js 3867 → 3440

| 文件 | 行数 |
| --- | ---: |
| `views/chatImages.js` | 263 |
| `views/suggestionsUi.js` | 163 |
| `views/stream.js` | 99 |

**「视图不等于『有弹窗的模块』」**：`stream.js` 不产出 DOM，只给
`streamPainter` / `initStreamFollow` / `scrollToBottom` —— 没有 `initXxx`。
凡是一段成套行为都可以是一个视图模块。

## 第十一批：拆 `viewSwitch` + `characterList` —— main.js 3440 → 3342

`viewSwitch.js` 55 行（页面级切换）、`characterList.js` 74 行（角色库列表页）；
同时把 `persistCharacters`（+26 行）沉进 `data/persist.js`。

**「读没读到」和「能不能写」是同一个约束的两面**：角色和世界书存两个文件，
主进程允许一次请求同时带上两者，但只在该带时才带 ——
`worldbooksLoaded` 为 false 时「存一次角色」会把 `worldbooks.json` 写空。
所以标志位必须和写函数住一起，否则角色编辑器 / 角色列表 / 导入三头又得点名它。

## 第十二批：拆角色编辑器一族 —— main.js 3342 → 2143

| 文件 | 行数 |
| --- | ---: |
| `views/characterEditor.js` | 882 |
| `views/charAttributes.js` | 343 |
| `views/characterImport.js` | 137 |

**兑现第六批欠的账**：`charEditorScope` / `editingCharacterId` 从入口层搬进
`views/characterEditor.js`（第六批注释里写明「留到这一刀一体处理」）。
搬完 `grep charEditorScope renderer/js/main.js` → 0 命中。

**这三个为什么能拆开（而不是像 `worldbook` 那样合并）**：
`charAttributes` 本身就是**另一个独立弹窗** `#char-attrs-modal`，不共用容器；
`characterImport` 用 `initCharacterImport({ openEditor, stashForm })` 注入，不回头 import。
判据同第六批：**共用容器的两块 UI 不能拆，不共用的就该拆。**
（对比第六批：`worldbook` + `worldbookChars` 共用同一容器 → 必须合并成 751 行一个文件。）

## 第十三批：拆完 `chat`，重构收尾 —— main.js 2143 → 482

| 文件 | 行数 | 文件 | 行数 |
| --- | ---: | --- | ---: |
| `views/composer.js` | 445 | `views/summarize.js` | 160 |
| `views/chatMessages.js` | 309 | `views/convoActions.js` | 144 |
| `views/chatExport.js` | 213 | `views/redraw.js` | 30 |
| `views/chatList.js` | 212 | `data/conversations.js` | 92 |
| `views/worldPlay.js` | 174 | | |

### 新招法：`redraw.js` 门面，替掉「给每个视图注入重绘函数」

拆到一半发现**几乎每个视图干完活都要「重画全屏」**。按老办法
（`initXxx({ rerender })`）入口层要给 8 个模块各塞一遍同样的回调，
而且以后每加一个模块都得回来补。

抽成公开门面后，视图直接 `import { renderAll } from './redraw.js'`：

```js
export function renderAll(options) {
  dropSuggestionsIfConvoChanged();   // 先问建议条：换了会话就不该继续挂着
  refreshAll(options);               // 再走刷新总线广播
}
```

关键：它**只依赖 `views/refresh.js` + `views/suggestionsUi.js`，不点名任何具体视图**
（绘制名单在刷新总线的登记表里）→ 所以不会成环。
这一改动把「19 对分区互相调用」里最主要的来源直接掐掉了。

### 三条刻意留在入口层（判据沿用第七批）

| 留 `main.js` | 为什么 |
| --- | --- |
| `api.onChunk` / `api.onReasoning` | 流式的**全局入口**，同时驱动消息列表 / 输入框状态 / 建议条，不属任何单视图 |
| Esc 的有序关闭链 | 「某个视图的实现」vs「一条有序链上的一环」→ 后者属入口层 |
| `importWorldbooks()` | 一次导入同时动角色库 / 世界书列表 / 角色列表 / 当前会话 → 跨视图编排 |

### 本批验证：diff 零行（逐字节一致）

最大的一刀（-1661 行）却是**验证最干净的一批**。原因就是第九批先把数据地基沉好了，
这一刀**只接线不重构**。和文档当初的预判一致。

> ⚠️ 提交 `0e502e4` 的信息写的是「2143 → 572 行」，但文件实际落到 **482 行**
> —— cleanup 掉未用 import 是在提交信息写完之后做的。**看数字以文件为准，别信提交信息。**

## 本轮踩的坑（可复用）

### 1. 引用模块引错 —— 用 `check-exports.js` 一次全量扫出来

两个实例，都是「名字对、模块不对」：

| 名字 | 误写 | 实际在 |
| --- | --- | --- |
| `MAX_PANEL_FIELDS` | `core/panel-fields.js` | `data/panel.js` |
| `syncConvoOptions` | `data/panel.js` | `data/suggestions.js` |

写了个 `check-exports.js`：把每个模块的 import 名和**目标模块的 export 名单**对账，
一次扫出全部错配。**比一个个跑 `npm run diag`（一次只报第一条）快得多。**

### 2. 「搬走的函数」和「它用到的东西」是两件事 —— 漏 import 的两个变体

| 漏的 | 症状 |
| --- | --- |
| `characterImport.js` 漏 `import { api }` | `api.importCard()` → 调 undefined |
| `chatMessages.js` 漏 `convoUserName` | `messageNode` 里 undefined |

搬完必须跑**双向**校验：`unused-imports.js`（有没有多引）+ `check-exports.js`（有没有少引）。

### 3. `cutblock.js` 的 `back` / `toBack` 修锚点漂移

「从锚点起找不到起点」= 起点标记离 `from` 锚点太远。加 `"back": 1` 往上退一行即可。
反过来，`to`（结束锚点）**误匹配到后面另一块注释的起始**时，把 `to` 换成更独特的行
+ `"toBack": 2` 往回收两行。

## 收尾（本轮完成）

- 文档同步：`README.md`（结构表 → 26 views / 13 data + 收尾注记）、
  `CHANGELOG.md`（补第八~十三批）、`重构方案.md`（进度、坑 7/8、规模表）
- `main.js` 头部注释：`还在往 ES module 拆` → `已按 ES module 分层拆完（峰值 7902 → 482 行）`
- 规模终值：**main.js 482 行 / data 13 个 / views 26 个**
- 验证：`npm run smoke` **431/431**、控制台 **0 报错**
- 临时脚本（`%TEMP%\cutblock.js` / `findcycle.js` / `unused-imports.js` / `check-exports.js`）
  保留可复用



## 项目优化评审（13:06）
- 星宝问项目还有哪些可优化（目标：做成类似酒馆 SillyTavern 的软件）。
- 评审结论：重构方案执行得很彻底，渲染层已收尾。剩余优先级：
  1. JSON 整份重写有崩溃/性能风险 → 建议原子写（tmp+rename），长期按会话分文件
  2. main.js 主进程还剩 store/providers/http/window/ipc 五块未拆（IPC 仍是 349 行巨型函数）
  3. 个人角色卡（Arryn.json、Adolion.json）混进 git 仓库，应 gitignore 或挪出
  4. 无 electron-builder 打包分发
  5. style.css 3007 行未拆（收益最小）
- 对标酒馆缺的功能方向：正则脚本、快速回复、提示词预设、作者注释、世界书更多触发策略。
- 待星宝确认后从 #1 原子写盘开始动手。
